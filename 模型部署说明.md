# 模型部署说明 · openPangu-R-7B-2512（昇腾 910B2C）

> 更新：2026-09-13 ｜ 状态：**已跑通**——权重就位、NPU 可用、慢思考/快思考两种模式均能出结果
> 配套：`AGENTS.md`（工作约定）、`环境说明.md`（机器与环境信息）

## 一、一句话结论

模型已在本机部署完成，用官方 HuggingFace 参考实现（不是 vLLM），
实测单卡 bf16 加载 16.6GB 显存，**进程预热后生成速度稳定在 36–40 tok/s**（详见 §五）。

```bash
source /data/openpangu/scripts/pangu_env.sh
/data/openpangu/.venv-pangu/bin/python scripts/pangu_infer.py "患者男，58岁，腰痛伴活动受限，直腿抬高试验阳性，最可能是什么病？"
```

## 二、为什么不是「容器自带环境 + vLLM」

这两条都是实测排掉的路，写下来避免重复踩。

**容器自带的 CANN 9.0.0 缺 910B 算子内核，NPU 根本算不了。**
`/usr/local/Ascend/cann-9.0.0/opp/built-in/op_impl/ai_core/tbe/kernel/config/ascend910b/` 下只有 `ops_oam`，
缺 `ops_math` / `ops_nn` / `ops_cv` / `ops_transformer` / `ops_legacy`（对比 310p 是齐的），
`opp/static_kernel` 也不存在。结果是连 `torch.ones(8,8).npu()+x` 都报
`Parse dynamic kernel config fail … AddAiCore ADD_TO_LAUNCHER_LIST_AICORE failed`。
另外 CANN 9.0.0 的 kernels 包在公开源上没有（`ascend-repo` 上 8.2.RC1、8.3.RC1 有 kernels-910b，9.0.0 只有 toolkit 和 nnal）。

**处理办法**：在 `/data/Ascend` 另装一套完整 CANN 8.3.RC1（toolkit 2.44GB + kernels-910b 2.24GB，装完 12GB），
环境变量走项目自己的 `scripts/pangu_env.sh`，不动容器原有的 9.0.0。

**vLLM 不能直接用来跑这个权重。**
vLLM 0.13.0 / 0.21.0 / 0.22.1 / 0.29.0 源码里的 `vllm/model_executor/models/openpangu.py`
只实现了 `param_sink` 注意力，没有 2512 的 `merge_conv`（FFN 因果卷积）、`attention_gate`、`groupnorm`，
而且它读 `interleaved_sliding_window`，2512 的 `config.json` 用的是 `layer_types`。
权重里这 84 个张量（`merge_conv.weight` / `attention_gate.weight` / `groupnorm.weight`）会被当"未使用"丢掉，
能起服务但不是这个模型，评测基线会失真。
官方 vLLM 配方（`inference/README_CN.md`）要求 CANN 8.3.RC1 + vLLM 0.11.0rc3 + vllm-ascend 0.11.0rc0 + 两个补丁 + 910B omni 自定义算子，
其中算子包只发布了 `linux-aarch64` 版本，本机是 x86_64，用不了。

## 三、实际环境清单

| 组件 | 版本 / 路径 | 说明 |
| --- | --- | --- |
| 权重 | `/data/models/openPangu-R-7B-2512`（17GB） | git-lfs 从 `atomgit.com/ascend-tribe/openPangu-R-7B-2512` 拉取；4 个 safetensors 分片 + tokenizer |
| CANN | 8.3.RC1，`/data/Ascend/ascend-toolkit/latest`（12GB） | 另装的一套，含 910B 算子内核；容器自带的 9.0.0 保持原样不动 |
| Python 环境 | `/data/openpangu/.venv-pangu`（Python 3.11.15） | 与 B 线 `.venv` 隔离，互不影响 |
| 推理框架 | torch 2.7.1+cpu、torch-npu 2.7.1、transformers 4.53.2 | 与 CANN 8.3.RC1 官方配套 |
| 其它依赖 | sentencepiece、einops、decorator、scipy、cffi、protobuf、absl-py | einops 是模型远端代码要的；decorator/scipy 等是 CANN 算子编译要的 |
| 环境变量 | `scripts/pangu_env.sh` | 手工写全，**不要** source 官方那个 `set_env.sh`（第一行有 bug，见下） |
| 推理入口 | `scripts/pangu_infer.py` | 命令行单轮问答 + 可 `import` 的 `PanguModel` |
| 冒烟脚本 | `scripts/smoke_pangu.py` | 首次验证用，输出思考过程与耗时 |
| 速度基准 | `scripts/bench_pangu.py` | 区分 prefill / decode 的测速脚本，`--sizes` 可调 |

## 四、怎么用

命令行：

```bash
source /data/openpangu/scripts/pangu_env.sh

# 慢思考（默认，会先输出思考过程）
/data/openpangu/.venv-pangu/bin/python scripts/pangu_infer.py "问题……"

# 快思考（更快，等价于在输入末尾加 /no_think）
/data/openpangu/.venv-pangu/bin/python scripts/pangu_infer.py "问题……" --fast

# 要结构化结果（含 thinking、token 数、耗时）
/data/openpangu/.venv-pangu/bin/python scripts/pangu_infer.py "问题……" --json
```

当模块调用（评测脚本用这个）：

```python
from scripts.pangu_infer import PanguModel

llm = PanguModel()
out = llm.chat("患者男，58岁，腰痛，直腿抬高试验阳性，最可能诊断？", fast_thinking=True)
print(out["content"])
```

## 五、实测数据（2026-09-13）

| 项目 | 实测值（两次独立复跑一致） |
| --- | --- |
| 模型加载 | 6.9 秒（4 个分片，权重在 /data） |
| 显存占用 | 加载后 16.57GB，预留 17.03GB，峰值 16.63GB（单卡 64GB，余量充足） |
| 进程首次调用 | 5–7 秒（昇腾算子首次编译），之后不再出现 |
| decode 速率（预热后） | **36–40 tok/s**：短问题 36.6–38.3，长输入（263 tokens 病例）37.9，慢思考 39.6 |
| 纯 decode 两点差分 | 33.3 / 39.1 tok/s（两次跑） |
| 短任务注意 | 只生成 100–200 token 时，5–7 秒的首次编译开销会把平均值拉到 14–25 tok/s，属正常现象，不代表稳态速度 |
| 慢思考 vs 快思考 | 速率基本相同（约 38 vs 39 tok/s），差别在输出长度：慢思考会先写思考过程，token 数更多、总耗时更长 |
| 样例 1 | "桶状胸 + 心尖搏动在剑突下 + P2 增强" → 答出 COPD/肺心病方向（与 `正式测评集` DX1-002 答案一致） |
| 样例 2 | "58 岁男性腰痛 + 直腿抬高阳性" → 答出腰椎间盘突出症（与 `正式测评集` DX1-001 答案一致），并给出鉴别诊断与 MRI 建议 |

复现命令：

```bash
source scripts/pangu_env.sh
.venv-pangu/bin/python scripts/bench_pangu.py              # 速度基准（约 1 分钟）
.venv-pangu/bin/python scripts/smoke_pangu.py --max-new-tokens 400   # 冒烟：两种模式各跑一次
```

评测估时（按 38 tok/s 算）：每题输出 300 token ≈ 8 秒，300 题 ≈ 40 分钟；
慢思考输出更长（实测 512 token 上限跑满），按 1000 token/题算 300 题约 2.2 小时。

### 五之二、单条慢在哪：显存带宽，不是算力

单条解码时 NPU 的 AICore 几乎闲着（利用率个位数），功耗 89W 左右；
每生成 1 个 token 都要把 15.5GB 权重从 HBM 完整读一遍——
37 tok/s × 15.5GB ≈ 574GB/s，只有卡上 HBM 带宽（约 1.6TB/s）的三分之一。
所以「资源没吃满」是必然的：**单条延迟由显存带宽决定，算力要批处理才吃得满**。

### 五之三、批处理实测（解决办法，零模型改动）

用 `PanguModel.chat_batch()` 并行跑多条序列，实测（128 token 输出，脚本 `scripts/bench_batch.py`）：

| batch | 总吞吐 | 单条吞吐 | AICore |
| --- | --- | --- | --- |
| 1 | 37.0 tok/s | 37.0 tok/s | 接近空闲 |
| 4 | 126.0 | 31.5 | — |
| 8 | 228.3 | 28.5 | — |
| 16 | 377.1 | 23.6 | — |
| 32 | 648.6 | 20.3 | — |
| 64 | 1158.4 | 18.1 | 72–80%，功耗 200–211W |
| 128 | 1795.5 | 14.0 | — |
| 256 | 2467.6 | 9.6 | — |

输出更长的情况（512 token）：batch 8 → 245 tok/s，batch 16 → 429 tok/s，batch 32 → 754 tok/s。

推论（评测提速）：300 题 × 512 token 输出，单条循环约 70 分钟，**batch 32 约 4 分钟**（约 16 倍）。

**可复现性注意**：批大小会轻微影响贪心结果（bf16 累加顺序不同，边界情况偶尔翻一个 token）。
实测：同一批配置下结果完全确定（同进程两次、跨进程两次指纹一致）；批内相同问题答案一致；
但 batch 1 与 batch 4 偶有差异。**评测要固定一个批大小并全程使用**，不要单条与批处理混用。

```python
from scripts.pangu_infer import PanguModel

llm = PanguModel()
out = llm.chat_batch([q1, q2, q3, ...], fast_thinking=True, max_new_tokens=512)
print(out["tokens_per_second"], "tok/s")   # 总吞吐
out["results"][0]["content"]               # 第一条的答案
```

## 六、已知坑（都踩过）

| 坑 | 说明 |
| --- | --- |
| 官方 `set_env.sh` 第一行是坏的 | CANN 8.3.RC1 生成的脚本里驱动路径带了个空格（`/usr/local/Ascend /usr/local/Ascend/driver/lib64`），会把 `LD_LIBRARY_PATH` 写坏，`torch_npu` 报 `libascend_hal.so: cannot open shared object file`。用项目里的 `scripts/pangu_env.sh` |
| 卡号必须写 0 | 容器只暴露 `/dev/davinci1`（宿主机 `npu-smi` 里编号 1），但容器内要设 `ASCEND_RT_VISIBLE_DEVICES=0`，设成 1 会报 `rtSetDefaultDeviceId … device id error` |
| 权重 LFS 缓存已清理 | 为腾磁盘，`.git/lfs/objects` 已删（省 17.6GB）。工作区里的权重文件完好；若将来误删要重下，用 `git lfs pull`（约 17.6GB） |
| 别在项目目录跑推理 | CANN 会在当前目录写 `fusion_result.json` 之类的临时文件，跑之前 `cd /data/openpangu` 即可，或跑完清掉 |
| 磁盘 | `/data` 59G 目前用 37G（权重 17G + CANN 12G + 环境 1.3G + 知识库语料与索引 0.6G），剩 19G。大文件一律放 `/data` |

## 七、进度与下一步

1. ~~用 `正式测评集/` 写评测脚本~~ **已完成**：评测方案与脚本在 `eval/`，见 `eval/README.md`；L0 基线结果见 `L0基线评测报告.md`（慢思考准确率 49.8%，快思考 44.9%）。覆盖 332 题里的 312 题（跳过 D07 模拟患者 15 题、D13 检索 5 题），三阶段（生成→判分→报告）+ 可选 rubric 判分。
2. ~~包一个 OpenAI 兼容 HTTP 服务~~ **已完成**：`scripts/pangu_server.py` + `scripts/serve_pangu.sh`，详见第八节。在线单条延迟仍是 37 tok/s 量级，需要更低延迟时再评估 vLLM 移植。
3. **下一步**：检索层（L2/L3）的对比实验——KB 索引已建好，`bash run_eval.sh --layer L2 --watch` 即可跑；
   提示词层（L1）与工具层（L4）、全链路（L5）的评测还要先把 `eval/` 接到 `agent.answer()`。

---

## 八、OpenAI 兼容服务（B 线接入）

```bash
# 起服务（加载 + 预热约 15 秒）
bash /data/openpangu/scripts/serve_pangu.sh start

# 用 B 线智能体连
cd /data/openpangu
OPENAI_BASE_URL=http://127.0.0.1:8000/v1 MODEL_NAME=openpangu-7b .venv/bin/python -m agent.cli

# 停止 / 看状态
bash scripts/serve_pangu.sh stop
bash scripts/serve_pangu.sh status
```

### 8.1 为什么需要这一层

模型**没有原生 function calling**：`tokenizer_config.json` 里的 chat template 只有
系统/助手/工具/方法/用户五种角色，没有 `tools` 参数。而 B 线智能体首轮就发
`tool_choice="required"` + 6 个工具（5 个业务工具 + `AgentTurnOutput` 结构化输出），
完全依赖 `tool_calls`。所以服务端做了两层桥接：

1. **工具轮**（快思考）：把工具目录渲染进提示词，要求模型按两行协议输出
   `TOOL: <名字>` / `ARGS: <JSON>`，再解析回 OpenAI 的 `tool_calls`。
   实测模型会漏写右花括号、多写 `}`、或照抄示例写成 `{{…}}`，解析层做了
   括号补全、半截键值对丢弃、双括号收敛等容错（`scripts/pangu_tool_bridge.py`）。
2. **收口轮**（快思考，独立一次调用）：让模型一步吐出 `AgentTurnOutput` 这种大嵌套
   JSON 几乎必然失败（实测要么继续调工具、要么说大白话），因此拆成一次专门的
   「格式转换」调用：给骨架 + 会话记录 + 回复草稿，只要求输出一个 JSON。

### 8.2 实测延迟（单卡、单请求串行）

| 场景 | 轮次 | 耗时 |
| --- | --- | --- |
| 直接收口（模型觉得不用工具） | 1 轮工具 + 1 轮收口 | 约 16 秒 |
| 调一次业务工具 | 2 轮生成 + 1 轮收口 | 约 25 秒 |
| 两轮连续问诊 | — | 25.3s + 26.7s（合计 52 秒） |

单条解码约 37 tok/s；一次用户轮次等于「工具轮(约 5s) × N + 收口轮(约 16s)」。
`--max-tool-rounds`（默认 3）控制工具轮上限，`--no-require-first-tool` 可关掉
「第一轮必须调一次工具」的补偿逻辑。

### 8.3 接口与错误语义

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/v1/chat/completions` | 支持 `tools` / `tool_choice`（`required` / `none` / 指定函数）/ 多轮 `tool` 消息；不支持 streaming |
| GET | `/v1/models` | 返回 `openpangu-7b` |
| GET | `/health` | 健康检查（`serve_pangu.sh status` 用它） |

- 400：请求体非法（缺 `messages` 等），错误体为 `{"error": {"message", "type"}}`；
- 500：生成异常，同样返回 OpenAI 风格错误体；
- 每次请求落盘到 `logs/pangu_server/<日期>.jsonl`（含耗时、命中工具、产出），便于复盘。
