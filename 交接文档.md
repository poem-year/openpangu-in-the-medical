# 交接文档 · openPangu 医疗诊断能力增强

> 更新：2026-09-13 ｜ 读者：接手本项目的 Codex 与队友
> 配套：`AGENTS.md`（工作说明）、`服务器环境说明.md`（机器信息）、`项目综述.md`（项目一页纸）

## 一、一句话现状

A、B 两条线已出一期成果并在本地验证通过；**C 线的模型服务刚具备开工条件，这台昇腾服务器就是给 C 线用的**。

## 二、进度盘点

### A · 知识库与检索（`rag_med_project/`）

已完成
- `retrieval.py`：BGE-M3 向量检索 + Chroma 持久化库，`retrieve(query, k)` 返回带出处片段
- 相似度阈值 0.8 过滤（注意 BGE-M3 的 score 越小越相似），查不到返回 `evidence: []`
- 接口与 `智能体接口规范.md` §2 一致

未完成
- 知识库里只有 2 个测试文档，真实语料（说明书、诊疗规范、huatuo 语料）未下载、未切块入库
- 混合检索（向量 + BM25 + RRF）与重排（bge-reranker-v2-m3）未做，即任务书里的 R2 / R3 路线
- 没有检索自测集（任务书要求 50 题）

### B · 智能体与提示词（`agent/`、`tests/`）

已完成
- LangGraph 编排 `agent/agent.py`、命令行 `agent/cli.py`、配置 `agent/config.py`、结构化输出 `agent/schemas.py`、调用记录 `agent/recorder.py`
- 4 个本地工具 + 1 个检索接口：`bmi` / `units` / `timeline` / `red_flags`
- 双层审查：规则层 `agent/review/rules.py` + 模型层 `agent/review/llm_review.py`
- 提示词 v1：`system_prompt_v1.md` / `review_fix_v1.md` / `emergency_templates_v1.md`
- 离线测试 83 项全绿（2026-09-13 本地复跑：`83 passed in 2.87s`）

未完成
- 未接真实模型端到端联调（一直用 `tests/fake_model.py` 假模型）
- 设计方案 §6.2 场景测试未跑，提示词 CHANGELOG 里回归结果仍是「待跑」
- 未在 Linux + Python 3.11 上验证过（本地是 Windows + Python 3.13）

### C · 模型服务与评测（`评测集/`）

已完成
- 测评集 v2：150 题，只考诊断
  - DX1 病例诊断选择 75 题、DX2 诊断性检查选择 15 题、DX3 病例诊断分析 14 题、DX4 鉴别诊断 26 题、DX5 多轮问诊结论 20 题
- 全部来自公开权威数据集（CMExam / CMB-Clin / IMCS-V2），无自编题
- 每题带 `level_hint` 与 `metrics`，对应 L0–L5；其中 90 题可机器判分，60 题按要点人工评分
- `answer_key.csv` 答题卡、`safety_items_source.json` 33 道自建红线补充题（可选）

未完成（= 服务器上的主任务）
- openPangu-7B 未部署，L0–L5 一层数据都没有
- 评测脚本未写（读题库 → 调模型 → 判分 → 出表）
- 演示界面（Streamlit）未做

## 三、下一步（按顺序做，不要跳步）

### 第 1 步 · 建 Python 环境（已完成 2026-09-13）

环境已就绪，直接用：

```bash
cd /data/openpangu
source .venv/bin/activate
python -m pytest -q          # 应输出 83 passed
```

实测记录
- `.venv` 建在 `/data/openpangu/.venv`（Python 3.11.15），`requirements.txt` 里钉的版本在 3.11 上**全部装成功**，不需要改版本
- 离线测试在服务器上同样全绿：`83 passed in 0.53s`
- pip 已配清华镜像（`pip config list` 可查）；要直连官方源时加 `-i https://pypi.org/simple`

### 第 2 步 · 部署 openPangu-7B（关键一步）

推荐组合（对应 CANN 9.0.0，避开 Docker、避开算子编译）：

| 组件 | 版本 |
| --- | --- |
| torch | 2.10.0 |
| torch-npu | 2.10.0 |
| vLLM | 0.21.0 |
| vllm-ascend | 0.21.0rc1 |

理由：vLLM 0.13 之后**原生内置盘古架构**（`vllm/model_executor/models/openpangu.py`），不需要官方补丁，也不需要自定义算子。

备选（官方老路径，只在推荐组合装不上时用）：CANN 8.3.RC1 + torch 2.7.1 + vLLM 0.11.0rc3 + vllm-ascend 0.11.0rc0 + omni 自定义算子。

权重：`openPangu-R-7B-2512`，4 个 safetensors 分片共 17.6GB，走 git-lfs：

```bash
git lfs install
git clone https://atomgit.com/ascend-tribe/openPangu-R-7B-2512.git /data/models/openPangu-R-7B-2512
```

注意：网页地址 `ai.atomgit.com` 会重定向，git 远端必须用 `atomgit.com`；权重必须放 `/data`。

目标：起一个 OpenAI 兼容接口（默认 `http://127.0.0.1:8000/v1`），供评测脚本和智能体共用。

### 第 3 步 · 跑 L0 基线

- 写评测脚本：读 `评测集/medical_diagnosis_eval_questions.jsonl` → 调接口 → 对照 `medical_diagnosis_eval.jsonl` 判分 → 出表
- 先跑 90 道选择题（DX1 / DX2）拿机器分，DX3–DX5 按要点人工评
- 结果记进 `分工文档/C_模型服务与评测.md` §二 那张实验表（编号 / 日期 / 层级 / 版本 / 题量 / 准确率 / 引用可追溯率 / 幻觉率 / 安全合规率）
- **L0 必须最先拿到**，它是后面五层提升的基线；没有 L0，后面说不清提升

### 第 4 步 · 端到端联调

- 把 `.env` 的 `OPENAI_BASE_URL` 指到本地接口，跑 `agent/cli.py` 走通一轮
- A 线语料入库后，跑 L2 / L3 对比

## 四、已知坑（都踩过）

| 坑 | 说明 |
| --- | --- |
| SSH 并发限制 | 平台限制同时会话数；连不上先等一分钟再试 |
| scp 被网关禁用 | 传文件用 `base64` 或管道，别用 scp |
| 容器内没有 Docker | 也装不上，服务只能裸装或换平台镜像 |
| 容器内不能建 user namespace | bubblewrap 沙箱不可用，Codex 必须以 `danger-full-access` 运行 |
| 系统盘小 | `/` 只有 30G，`/data` 59G，大文件一律放 `/data` |
| 实例销毁 = 全清 | 只有 `/root` 与 `/data` 跨镜像保留；关机保 36 小时 |
| 驱动镜像要选对 | 按 openEuler 24.03 + Python 3.12 + CANN 9.0.0 选，别选 CANN 8.x |
| 旧模型名已下线 | DeepSeek 的 `deepseek-v4-flash` 已改名为 `deepseek-flash`，配错会起不来 |

## 五、待确认事项（需要人拍板）

1. `评测集/key_points` 是脚本自动抽取的，正式评分前需医学背景的人过一遍（重点 DX3 / DX4）
2. 是否保留 33 道自建红线安全题：若要求全部题源公开，直接删 `safety_items_source.json`，主体 150 题不受影响
3. 模型选型：主用 7B；1B 作为显存/速度对照；7B-VL 暂不涉及