# 脚本地图 · 哪个该用、哪个别碰

> 2026-09-14 整理 ｜ 全项目脚本一览。**要跑测评只看第 1 节的三条命令**，
> 其余是内部实现或历史遗留。

## 一、你要用的（正式入口，只有三个）

| 目的 | 命令 | 说明 |
| --- | --- | --- |
| **跑测评** | `bash eval/scripts/run.sh eval/configs/<实验>.yml` | 唯一入口。建不可变 run 目录 → 跑引擎 → 归档报告 → 追加登记表 |
| **看实时面板** | `bash eval/scripts/watch.sh` | 默认看最新一次；`--all` 看所有在跑的轮次（含 NPU/磁盘） |
| **看报告** | 打开 `评测报告/INDEX.md` 找那一行，或直接看 `评测报告/<run-id>/summary.md` | 网页版是 `report.html`，原始数据在 `eval/runs/<run-id>/` |

配套两条：`bash eval/scripts/run.sh --list` 看有哪些实验；`bash eval/scripts/stop.sh` 停评测。

## 二、实验配置（加实验不用改代码）

| 文件 | 引擎 | 说明 |
| --- | --- | --- |
| `eval/configs/l0-baseline.yml` | direct | 裸模型基线 |
| `eval/configs/l3-hybrid.yml` | direct | RAG 混合检索，4 倍预算 |
| `eval/configs/agent-4x.yml` | agent-batch | 智能体全链路，批处理，RAG 3 条 |
| `eval/configs/agent-smoke.yml` | agent-batch | 冒烟：每维 2 题、不跑 AI 判分 |

## 三、引擎与框架（改代码时看这里）

| 路径 | 作用 | 状态 |
| --- | --- | --- |
| `eval/harness/` | **框架**：配置解析、不可变 run、报告归档、登记表 | ✅ 正式 |
| `eval/run_eval.py` | 直连引擎（L0–L5）：生成 → 判分 → 报告 | ✅ 正式 |
| `eval/run_agent_batch.py` | 智能体引擎：按轮次批处理（快 4 倍） | ✅ 正式 |
| `eval/eval_lib.py` | 题库加载、提示词、规则判分、口径适配 | ✅ 正式 |
| `eval/ai_judge.py` / `eval/judge.py` | DeepSeek 判分（开放题 / rubric），带缓存 | ✅ 正式 |
| `eval/traceability.py` | 引用可追溯率（可定位 / 可支撑） | ✅ 正式 |
| `eval/show_report.py` / `eval/compare.py` | 出报告 / 多轮对比 | ✅ 正式 |
| `eval/watch.py` / `eval/progress.py` | 看板渲染与进度落盘 | ✅ 正式 |
| `eval/run_agent_eval.py` | 逐题串行调智能体（18–24 秒/题） | 🟡 保留：慢，仅作对照 |
| `eval/scripts/run.sh` `watch.sh` `stop.sh` | 三个入口脚本 | ✅ 正式 |

## 四、模型侧（`scripts/`）

| 路径 | 作用 | 状态 |
| --- | --- | --- |
| `scripts/serve_pangu.sh` | 起/停 OpenAI 兼容服务（`start`/`stop`/`status`/`restart`） | ✅ 常用 |
| `scripts/pangu_server.py` | 服务本体：`/v1/chat/completions` + `/v1/batch/chat`（批处理） | ✅ 正式 |
| `scripts/pangu_tool_bridge.py` | 工具协议渲染与容错解析（纯逻辑） | ✅ 正式 |
| `scripts/pangu_infer.py` | 推理入口与 `PanguModel`（含 `chat_batch` / `chat_batch_messages`） | ✅ 正式 |
| `scripts/pangu_env.sh` | 推理所需环境变量（CANN 路径、卡号） | ✅ 必 source |
| `scripts/bench_pangu.py` / `bench_batch.py` | 单条 / 批量测速 | 🟡 需要时用 |
| `scripts/smoke_pangu.py` / `smoke_agent_server.py` | 换机器后的冒烟 | 🟡 需要时用 |

## 五、知识库侧（`kb/`）

| 路径 | 作用 |
| --- | --- |
| `kb/build.py` | 建库 CLI（增量 / `--rebuild` / 两段式切块+嵌入） |
| `kb/inspect.py` | 查一条，看 top-k、余弦、BM25 名次、来源 |
| `kb/calibrate.py` | 阈值标定（Recall@5 vs 误召率） |
| `kb/eval_set.py` | 从语料反向生成检索评测集候选 |
| `kb/eval_retrieve.py` | L2/L3 预检索（写 `contexts.jsonl`，跑在 `.venv`） |
| `kb/search.py` / `index.py` / `bm25.py` / `embed.py` / `corpus.py` / `config.py` | 检索实现与配置 |
| `kb/corpus_raw/_scripts/*.py` | 语料采集与转换（换机器重建语料时用） |

## 六、环境与同步

| 路径 | 作用 |
| --- | --- |
| `setup/restore.sh` | 换服务器一键恢复环境（CANN + 权重 + 两个 venv + 冒烟） |
| `sync.sh` | `git add -A` 提交并推送（**推之前先 `git add -A --dry-run`**，防大目录进仓） |
| `评测报告/collect.sh` | 把某个 run 的报告快照到 `评测报告/`（旧脚本跑出来的轮次补档用） |

## 七、历史遗留（`legacy/`，别在新流程里用）

| 脚本 | 原用途 | 现在用什么 |
| --- | --- | --- |
| `legacy/run_eval.sh` | 一键评测（L0 慢/快 + 后台看板） | `bash eval/scripts/run.sh <配置>` |
| `legacy/watch.sh` | 单 run 看板 | `bash eval/scripts/watch.sh` |
| `legacy/watch_all.sh` | 多 run 看板 | `bash eval/scripts/watch.sh --all` |
| `legacy/stop_eval.sh` | 停评测 | `bash eval/scripts/stop.sh` |
| `rag_med_project/` | 队友交付的旧版检索（Chroma） | `kb/`（只在做对照时打开） |
| `inbox/2026-09-13/` | 队友原始交付包 | 原样留档，不改 |

## 八、一张图

```
你 → eval/scripts/run.sh <配置>
        │
        ├─ harness：建 run 目录（UTC 时间戳+实验名+指纹，不覆盖）
        ├─ 引擎：direct → eval/run_eval.py
        │        agent-batch → eval/run_agent_batch.py ──HTTP──> scripts/pangu_server.py ──> NPU
        │        （检索走 kb/，判分走 eval/judge.py + ai_judge.py）
        └─ 产出：eval/runs/<run-id>/        过程数据（不进 git）
                  评测报告/<run-id>/        报告（进 git）
                  评测报告/INDEX.md         登记表（只追加）

看进度：eval/scripts/watch.sh [--all]
看报告：评测报告/INDEX.md → 点进那一行
```
