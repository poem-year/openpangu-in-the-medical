"""长期测评框架：一次实验 = 一份配置 + 一个不可变的 run 目录 + 一份不可变的报告。

设计原则（见 `评测框架.md`）：
1. **不覆盖**：run id 带 UTC 时间戳与配置指纹，目录已存在就拒绝启动；
2. **可复现**：每次运行把配置、参数、git commit、数据集指纹写进 `meta.json`；
3. **分工清晰**：引擎在 `eval/*.py`（怎么算），框架在 `eval/harness/`（怎么组织）；
4. **过程数据与报告分开**：过程数据 `eval/runs/<run-id>/`（不进 git），
   报告 `评测报告/<run-id>/`（进 git），登记表 `评测报告/INDEX.md` 只追加。
"""

__all__ = ["config", "store", "runner"]
