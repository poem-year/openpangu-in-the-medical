# 医疗诊断智能体 · 设计方案

> 版本 v1.0 · 2026-09-12
> 配套文档：《智能体接口规范.md》（函数签名、字段、枚举以配套文档为单一来源）
> 定位场景：中国国际大学生创新大赛（2026）· 华为命题「面向医疗诊断场景的 openPangu 能力增强」· 智能体模块（B 线）

---

## 0. 文档说明

### 0.1 本文回答什么
- 要造一个什么样的智能体：定位、范围、边界（§1）
- 它内部由哪些机制组成、为什么这样设计：诊断思维、病历卡、问诊沟通、工具、双层审查、表达（§2–§3）
- 提示词体系怎么组织与版本化（§4）
- 工程上怎么落地：模块划分、配置、运行、中间件选型（§5）
- 怎么测、怎么验收、分几期上（§6）
- 安全底线：危险信号与红线清单 v0.1（附录 A）

### 0.2 本文不包含什么
- 不含具体代码实现（本轮只交设计，落地在下一轮）
- 不含提示词全文（只写结构要点与版本管理方式，见 §4）
- 不含检索与知识库建设方案（A 负责；接口见《智能体接口规范.md》§2）
- 不含模型部署与微调（C 负责；接口见《智能体接口规范.md》§3）

### 0.3 读者与用法

| 读者 | 重点章节 |
| --- | --- |
| 本组开发（智能体模块） | 全文 |
| A（检索） | §2.4、附录 A.1（检索服务如何被调用） |
| C（模型服务） | §5.1、§5.4，《智能体接口规范.md》§3 |
| 评测 / 界面 | §3.6、§6.2，《智能体接口规范.md》§4 |

### 0.4 版本记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v0.2（描述方案，评审通过） | 2026-09-12 | 「诊断者」定位、假设-检验循环、病历卡、双层审查、患者化表达 |
| v1.0（本文，设计版） | 2026-09-12 | 在 v0.2 基础上完成实施级设计；接口细节移至《智能体接口规范.md》 |

---

## 1. 定位与范围

### 1.1 一句话定位

面向患者（普通成年人）的**诊断辅助智能体**：模型即诊断者，通过多轮问诊主动采集信息、维护结构化「病历卡」、迭代假设-检验循环，必要时调用工具与检索接口，经安全审查后，以患者能理解的语言输出「评估 + 行动建议」。

### 1.2 它是什么样的智能体（四个特征）

1. **是诊断者，不是问答机**：目标是尽可能把问题想清楚，而不是尽快给出答案。
2. **有结构**：症状→时间线→背景→假设→证据→缺口，每一步都显式记录、可检查、可评测。
3. **有底线**：三类红线硬拦截；不确定时一律向「更保守、更建议就医」兜底。
4. **说人话**：医学质量优先的前提下，表达为患者服务。

### 1.3 做与不做

| 维度 | 本期做 | 本期不做 |
| --- | --- | --- |
| 使用者 | 患者侧：普通成年人、非医学背景 | 医生工作站、临床决策支持系统 |
| 场景 | 成人常见症状的问诊式初步评估 + 就医建议 | 心理危机/精神疾病专项、儿童、孕产专项评估 |
| 知识 | 预留检索接口（工具调用 + mock） | RAG / 知识库建设（A 负责）；不做知识图谱 |
| 输出 | 文本回复 + 结构化评估（assessment） | 确诊结论、处方与剂量 |
| 记忆 | 会话内对话历史 + 病历卡 | 跨会话长期记忆、用户画像（隐私更好，本期不做） |
| 部署 | 单机 CLI（开发期）；模型走 OpenAI 兼容接口 | 多租户、高并发服务化（不要求可迁移性） |

### 1.4 设计原则（按优先级排序）

1. **安全优先**：任何取舍先问「最坏情况会不会伤人」；兜底方向只有一个——更保守、更建议就医。
2. **思维显式**：诊断过程结构化，假设、证据、缺口全部落字段，能被审查与被评测。
3. **诚实第一**：不知道就说不知道，没有依据就说没有依据；不编造引用，不假装确定。
4. **患者视角**：表达可懂、有温度、有节奏；但话术不得牺牲医学准确性——**质量优先于话术**。
5. **工程从简**：不引入论文中复杂而脆弱的方法；每个机制都要能落成代码、能被单测。目标——先构建出一套正常工作的智能体。

### 1.5 合规与免责边界（工程约束）

- 产品定位是「辅助」：不构成诊断行为本身；所有输出默认包含「最终请以医生面诊为准」。
- 不采集可识别个人身份的信息（姓名、证件号、联系方式等）；首次会话给出隐私提示。
- 急症信号出现时引导急诊 / 拨打 120，不做任何延误性表述。
- 附录 A 的红线清单为工程初稿，**正式上线前必须经医学专业人员评审 + 合规审阅**。

---

## 2. 总体架构与标准流程

### 2.1 运行时骨架

选用 LangChain 官方 `create_agent`（已装 1.4.0，实测可用）作为智能体主循环，LangGraph 提供状态与图能力：

- **模型**：`ChatOpenAI`（OpenAI 兼容）接 C 的模型服务；temperature 固定 0.1。
- **工具**：4 个本地确定性工具 + 1 个检索接口工具（mock 起步，A 的实现按同签名替换）。
- **结构化输出**：`response_format`（ToolStrategy 路径）——每轮产出 `AgentTurnOutput`（reply + assessment + case_card + risk_level），落在图输出的 `structured_response` 字段（源码已核实）。
- **会话状态**：对话历史（消息序列）为载体；CLI 亦可选用 `InMemorySaver` 承载长会话（已实测可用）。
- **中间件**：一期只启用少量稳定性中间件（见 §5.5），其余官方中间件按需再开、宁缺勿滥。

```
┌───────────────────────────────────────────────────────────────┐
│ 入口层  answer(question, history) → AgentResult               │
│   · 前置安全扫描（代码）：红线/急症关键词 → 命中即转应急分支     │
│   · 轮数控制：达到上限时注入「收口指令」                        │
├───────────────────────────────────────────────────────────────┤
│ 智能体主循环（create_agent）                                   │
│   模型 ⇄ 工具（BMI / 单位换算 / 红旗核对 / 时间线 / 检索）      │
│   状态：对话历史 + 每轮结构化输出（含病历卡快照）               │
├───────────────────────────────────────────────────────────────┤
│ 审查层（review）                                               │
│   一期：代码硬拦截（确定性规则）                                │
│   二期：模型复核（语义检查，预留）                              │
│   动作：放行 / 修正重生成 / 降级措辞 / 替换为就医建议            │
├───────────────────────────────────────────────────────────────┤
│ 表达组装 → AgentResult{reply, assessment, risk_level, ...}    │
└───────────────────────────────────────────────────────────────┘
```

### 2.2 标准处理流程（每一轮对话）

1. **前置安全扫描（代码，毫秒级）**：扫描本轮用户输入中的急症 / 自伤信号关键词。
   - 命中急症 → 跳过问诊，直接返回应急指引（`handled_as = "emergency"`），同时记录触发词。
   - 命中自伤/伤人表达 → 返回安全转介话术（本期不做心理评估，只做安全转介）。
2. **组装本轮输入**：history + 本轮问题；首次对话附隐私提示要求（提示词层规定）。
3. **模型主循环**：模型按需调用工具（红旗核对、单位换算、时间线计算、检索等），持续更新对病例的理解。
4. **结构化产出**：模型输出 `AgentTurnOutput`：
   - `case_card`：更新后的病历卡完整快照（§3.2）
   - `assessment`：结构化评估（hypotheses / evidence / missing_info / next_steps / confidence）
   - `reply`：拟对患者说的话
   - `risk_level`：风险等级（low / medium / high / emergency）
5. **审查层处理**（§3.5）：结构校验 → 硬拦截规则 →（二期）语义复核 → 命中动作。
6. **收口检查（代码）**：若本轮为收口轮（给出倾向性结论或达到轮数上限），校验收口条件（§3.1）；不满足则驳回重生成或降级。
7. **返回与记录**：组装 `AgentResult` 返回调用方；记录 `used_tools`、审查命中日志（供评测统计）。

### 2.3 降级原则（贯穿全流程）

| 异常情形 | 降级行为 |
| --- | --- |
| 检索服务不可用 / 返回空 | 如实告知「未查到依据」，不做知识性断言；对话继续 |
| 工具报错 | 错误信息返回给模型，模型向用户说明后可继续问诊；不允许带病计算 |
| 模型输出结构不合法 | 带修正指令重试 1 次；仍失败则返回兜底话术 |
| 审查命中违规 | 修正重生成（1 次）→ 仍违规则替换为保守话术 / 就医建议 |
| 模型服务不可用 | 向调用方抛出明确异常；CLI 给出友好提示（不静默失败） |

一句话总结：**所有降级都往「更保守」方向走，绝不往「更大胆」方向走。**

### 2.4 组件职责与边界

| 组件 | 职责 | 边界 |
| --- | --- | --- |
| 智能体核心（B） | 主循环、状态、工具、审查、表达、CLI | 不做检索实现、不做模型部署 |
| 检索（A） | `retrieve(query, k) -> list[Evidence]` | 只按接口供给证据；不直接面向患者 |
| 模型服务（C） | OpenAI 兼容推理接口 + 评测数据 | 必须保证 tools 可用（见接口规范 §3.2） |
| 评测 / 界面 | 消费 `AgentResult` | 只依赖公开输出字段，不依赖内部状态 |

---

## 3. 核心机制设计

### 3.1 诊断思维循环（假设-检验-缺口）

**依据**（论文调研结论 → 工程设计映射）：

| 研究结论 | 来源（arXiv） | 本设计如何落地 |
| --- | --- | --- |
| 诊断对话应按「病史采集 / 诊断准确性 / 处置建议 / 沟通 / 共情」多轴评估 | 2401.05654（AMIE） | 评测五轴（§6.2C）+ 表达层设计（§3.6） |
| 迭代「假设生成-追问-修订」优于一次性问答 | 2506.22405（序贯诊断） | 每轮强制更新假设集（含反证栏），不许原地不动 |
| 信息获取三大失误：凑合 / 锚定 / 过早收口 | 2607.10275 | 三条对应防御机制（见下） |
| 推理-行动-观察循环是工具型智能体的基本范式 | 2210.03629（ReAct） | create_agent 主循环 + 工具调用 |

**循环步骤**（每轮对话内跑一遍）：

1. **入档**：把本轮新信息并入病历卡（症状属性、时间、背景、用药……）。
2. **假设生成**：形成 / 更新候选假设列表（一般 1~4 个，按可能性降序）。
3. **假设检验**：为每个假设列「支持证据 / 反对证据」；出现强反证必须降级或剔除该假设。
4. **缺口识别**：当前最不能确定的是什么（对应 `missing_info`）。
5. **询问决策**：优先问「能区分前两位假设」的问题；其次补齐红旗筛查。
6. **判定继续或收口**：满足收口条件才允许给出结论性内容。

**三条防御机制（对应三大失误）**：

- **防凑合**：信息不足时，必须输出「目前不足以区分 X 和 Y」并列出还缺什么；禁止用模糊话术假装已清楚。
- **防锚定**：每个假设必须携带「反对证据」栏；若连一条反对证据都写不出，提示词要求模型自查是否被首因效应锚定。
- **防过早收口**：收口条件不满足时，审查层驳回结论性输出（见 §3.5 收口检查）。

**收口条件**（同时满足才允许给出「倾向性判断」）：

| 条件 | 说明 |
| --- | --- |
| a. 红旗扫描完成 | 已问询、或有明确排除记录 |
| b. 假设有据 | 主要假设有对话/工具/检索证据支撑（`evidence` 非空） |
| c. 缺口已交代 | 关键待排查项已覆盖，或已明说「无法覆盖，需就医确认」 |
| d. 轮数上限特例 | 达上限时允许收口，但 `confidence` 不得为 high，且 `next_steps` 必须含就医建议 |

（一期以「提示词要求 + 审查层轻量校验」实现 a/c；完整语义判定在二期模型复核中加强。）

### 3.2 「病历卡」（结构化记忆）

**为什么需要**：多轮问诊信息易散、易丢；结构化病历卡让「不遗漏」成为机制而不是愿望，也让审查层有据可查。

**存储与更新**：

- 载体：每轮结构化输出中的 `case_card` **完整快照**（不是增量）；模型基于对话历史维护，可随时回溯重建。
- 持久性：随对话历史停留在会话内；本期不做跨会话持久化（隐私 + 从简）。
- 代码侧职责：schema 校验 + 基础一致性检查（如红旗字段非空时 `risk_level` 不得低于 high）；不做字段合并逻辑（从简）。

**字段设计**：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `age_group` | str | 年龄区间（如 `30-39`）；不采集精确身份信息；未提供填「未提供」 |
| `sex` | str | 男 / 女 / 其他 / 未提供 |
| `chief_complaint` | str | 主诉（一句话） |
| `symptoms` | list[str] | 症状条目：部位/性质/程度/持续时间/诱因/缓解因素等已知属性 |
| `timeline` | list[str] | 关键事件时间线（起病、演变、就医/用药），按时间排序 |
| `context` | list[str] | 既往史、过敏史、长期用药、家族史、生活习惯（按需采集） |
| `red_flags_found` | list[str] | 已识别的危险信号（含发现时间/来源轮次） |
| `red_flags_excluded` | list[str] | 已确认排除的危险信号（需有依据，如「无胸痛」） |
| `hypotheses` | list[Hypothesis] | 假设集（与 assessment 同源，见接口规范 §1.3） |
| `open_questions` | list[str] | 当前最值得问的问题候选 |

**防遗漏设计**：提示词要求模型每轮结束前对照字段清单自查（哪个字段还空着、哪个症状属性没问全）；审查层对必经字段（主诉、红旗）做存在性校验。

### 3.3 问诊与沟通（听 / 探 / 确认 / 建议）

**不设死脚本**（不规定第几轮必须说什么），只给四种对话功能与约束：

| 功能 | 作用 | 设计要点 |
| --- | --- | --- |
| 听 | 接收并复述患者信息 | 先复述确认再追问；避免连珠炮式提问 |
| 探 | 有方向地追问 | 一次聚焦一个主题；问题优先服务于「区分假设」与「红旗筛查」 |
| 确认 | 阶段性小结，请患者纠偏 | 每 2~3 轮做一次小结（「我目前理解的是……对吗？」） |
| 建议 | 给出下一步行动 | 四档必须明确：观察 / 近日就医 / 尽快就医 / 立即急诊 |

**节奏约束（软性，可配置）**：

- 默认最多 6 轮追问（`AGENT_MAX_TURNS`），达到上限按 §3.1 条件 d 收口；
- 单次回复内提问数建议 ≤2 个；
- 急症不等待、不追问，直接应急分支。

**患者化表达原则**（细则在 §3.6）：共情先行但克制（不表演共情）；术语出现即解释；坏消息要直接但必须携带行动建议；不确定要说明「不确定到什么程度、由什么决定」。

（以上为原则与边界；具体话术风格在提示词中定义，提示词只写结构要点——见 §4。）

### 3.4 工具设计（4 个本地工具 + 1 个检索接口）

**总原则**：

- 工具只做「确定性计算 / 规则核对 / 检索」，不做医学判断（判断留给模型）；
- 所有工具返回结构化结果；出错返回错误对象，**不抛异常中断对话**；
- 模型不得伪造工具结果；未调用工具不得声称「已核对 / 已换算」。

| 工具 | 用途 | 输入 → 输出 | 错误与降级 |
| --- | --- | --- | --- |
| `calculate_bmi` | 成人 BMI 计算与分级 | (weight_kg, height_cm) → BMI 值 + 分级（中国成人标准） | 数值超出生理合理范围 → 返回错误，提示核对输入 |
| `convert_units` | 常用单位换算 | (value, from_unit, to_unit) → 换算结果 | 不支持的换算对 → 返回支持列表；无法解析 → 请求澄清 |
| `check_red_flags` | 红旗清单核对 | (symptoms: list[str]) → 命中条目 + 建议等级 | 无命中 → 「未命中（不代表安全）」；输入为空 → 提示补充 |
| `timeline_calc` | 时间线整理与病程计算 | (events: list[str]) → 排序后的时间线 + 距今天数/间隔 | 无法解析的条目 → 单独返回，请求澄清 |
| `retrieve_evidence` | 检索资料（接口预留） | (query, k=5) → list[Evidence] | 空结果/异常 → 如实告知未查到；当前 mock 返回空列表 |

补充口径：

- `calculate_bmi`：分级按中国成人标准（<18.5 偏瘦；18.5–23.9 正常；24–27.9 超重；≥28 肥胖）；仅适用成人；输出附「分级不能替代评估」。
- `convert_units`：支持长度（cm/m/英寸/英尺）、重量（kg/斤/磅）、温度（°C/°F）；**药品剂量换算不在支持范围**（主动排除高风险用途）。
- `check_red_flags`：规则数据内置（附录 A 的代码化，单一来源），关键词/模式匹配；返回每条命中的名称、类别、建议动作。
- `timeline_calc`：支持 ISO 日期（YYYY-MM-DD）与「N 天/周/月/年前」相对描述；输出排序时间线与相邻间隔。
- `retrieve_evidence`：面向模型的工具名；底层调用 A 的 `retrieve()`（当前 mock 返回 `[]`）；契约详见《智能体接口规范.md》§2。

### 3.5 审查与安全（双层）

#### 一期：代码硬拦截（确定性，全部可单测）

| 检查项 | 内容 | 命中动作 |
| --- | --- | --- |
| 自伤/伤人表达 | reply 中出现相关表达的处理兜底 | 替换为安全话术模板 |
| 确诊措辞 | 「你得了 / 确诊为 / 就是 XX 病」等模式 | 驳回重生成 → 降级措辞 |
| 用药违规 | 具体药物推荐 / 剂量 / 停药建议模式 | 驳回重生成 → 替换边界话术 |
| 绝对化承诺 | 「一定能 / 肯定没事 / 包治」 | 降级措辞 |
| 红旗一致性 | `red_flags_found` 非空但 `risk_level` < high，或 `next_steps` 无就医字样 | 上调 risk_level + 追加就医建议 |
| 结构校验 | 字段完整性、枚举合法性、非急症分支 `hypotheses` 非空 | 重试 1 次 → 兜底话术 |
| 引用防伪 | `evidence.source="kb"` 的 ref 必须是本轮检索工具真实返回过的 chunk_id | 删除该条证据并驳回 |
| 收口检查 | 收口轮未满足 §3.1 收口条件 | 驳回并注入「继续问诊 / 补充说明」指令 |

#### 二期：模型复核（预留）

- 证据覆盖：每条主要假设是否有支撑、支撑是否来自对话/工具/检索；
- 编造检查：检索为空却声称「有资料/研究表明」；
- 措辞-把握度一致性：confidence=low 却使用确定措辞；
- 收口质量：结论与缺口交代是否匹配。

复核模型可与诊断模型相同或更强（配置化 `REVIEW_MODE=code+llm`）；复核输出结构化，动作复用一期的动作集合。

#### 动作定义（按严重度）

1. `pass`：放行；
2. `fix`：带修正指令重生成一次（附具体违规点）；
3. `degrade`：代码级改写、下调措辞（如「确诊」→「目前考虑 / 不能排除」）；
4. `replace`：直接替换为应急 / 就医话术模板（高危场景，不再经过模型）。

#### 日志

每次命中记录 `{轮次, 检查项, 动作, 命中片段}`，供评测统计与提示词迭代。

### 3.6 表达层（患者化输出）

- **两段式**：先给「结论 + 建议」主信息（1~3 句），再补充依据与细节；患者不读第二段也不影响安全。
- **关键信息前置**：风险等级与行动建议必须出现在回复前半段。
- **分节奏**：问诊中每轮聚焦一个主题；小结轮只做确认不做结论；收口轮按「目前考虑 / 主要依据 / 还缺什么 / 建议怎么做」四段骨架展开。
- **语言原则**：短句；少术语（出现即解释）；不吓人也不轻描淡写；给出确切行动（做什么、多久内、去哪）。
- **禁止清单**：确诊式表达、剂量处方、绝对化承诺、贬损表述、用「安慰式」表述掩盖不确定性。
- **结构字段不外显**：`assessment` 是机器可读层，默认不直接展示给患者（界面可提供「展开详情」）。

---

## 4. 提示词体系（结构要点）

### 4.1 提示词清单（拟）

| 文件（拟） | 用途 |
| --- | --- |
| `prompts/system_prompt_v1.md` | 主系统提示：角色、边界、思维步骤、输出要求、安全规则、示例 |
| `prompts/review_fix_v1.md` | 审查驳回后的修正指令模板（由代码注入） |
| `prompts/emergency_templates_v1.md` | 急症 / 自伤 / 越界请求的固定话术（代码调用，非模型生成） |
| `prompts/review_llm_v1.md`（二期） | 模型复核提示 |

### 4.2 主提示结构要点（只列结构，不写全文）

| 模块 | 写什么 | 要点 / 易错点 |
| --- | --- | --- |
| 角色与定位 | 你是谁（诊断者）、服务谁（患者）、不是什么（不替代医生） | 明确「辅助」定位；禁止扮演真人医生 |
| 边界与红线 | 三类红线浓缩版 + 动作（拒答 / 转介） | 与附录 A 保持一致；措辞不模糊 |
| 思维步骤 | 假设-检验-缺口-询问-收口循环（§3.1 的文字化） | 强调「每轮必须更新假设，且带反证栏」 |
| 病历卡维护 | 每轮输出完整快照；字段清单与自查要求 | 强调不遗漏：对照字段逐项检查 |
| 工具使用 | 何时必须调用（红旗核对、换算、检索…）；不得伪造结果 | 写清工具名、用途与调用时机 |
| 输出格式 | `AgentTurnOutput` 结构 + 字段语义 + 枚举值 | 以《智能体接口规范.md》为单一来源 |
| 安全规则 | 红旗→就医；不确定→如实说明；不确诊、不开药 | 每条禁令配 1 个反例、关键行为配 1 个正例 |
| 示例 | 2~3 个短示例（常规问诊 / 红旗处置 / 拒绝越界） | 示例要短，防止模型照抄 |

### 4.3 写作要求与版本化

- 用「行为句」不用「感悟句」：写「当 X 时，做 Y」，不写「要专业、要贴心」这类空话。
- 每条禁令配反例；每条关键行为配正例（正例可取自真实测试会话）。
- 文件名带版本号（`*_v1.md`、`*_v2.md`）；目录内维护 `CHANGELOG.md`（列：日期 / 版本 / 变更 / 原因 / 回归结果）。
- 提示词变更视为代码变更：必须跑回归场景（§6.2B）后才允许升版本。

---

## 5. 工程实现建议

### 5.1 技术栈与环境（已就绪）

| 项 | 版本 / 说明 |
| --- | --- |
| Python | 3.13（`.venv` 已建好） |
| langchain | 1.4.0（`create_agent`、中间件体系实测可用） |
| langgraph | 1.2.11（`InMemorySaver` 实测可用） |
| langchain-openai | 1.6.2（`ChatOpenAI`，接 C 的 OpenAI 兼容服务） |
| 测试 | `pytest`（开发阶段再装，属开发依赖） |
| 追踪（可选） | langsmith 0.12.4（随 langchain 安装，用于调试） |

已实测的关键事实（写作依据）：

- `create_agent` 全部所需参数可用（`model / tools / system_prompt / middleware / response_format / checkpointer / state_schema` 等）；
- 官方中间件 16 个类可导入；`InMemorySaver`、`GenericFakeChatModel` 可用；
- 假模型可驱动主循环冒烟；但 `GenericFakeChatModel` **不支持 `bind_tools`**——涉及工具调用与结构化输出的测试，需要一个约 40 行的「脚本化假模型」子类（B 侧测试资产）。

### 5.2 模块划分（拟）

```
agent/                     # 智能体模块（B 线）
├── config.py              # 读取 .env 与默认参数（模型、轮数上限、审查开关）
├── schemas.py             # 全部 Pydantic 模型（单一来源）：
│                          #   AgentResult / AgentTurnOutput / Assessment /
│                          #   Hypothesis / EvidenceItem / CaseCard
├── prompts/               # 版本化提示词（v1 起步）
├── tools/
│   ├── bmi.py             # calculate_bmi
│   ├── units.py           # convert_units
│   ├── red_flags.py       # check_red_flags（规则数据在 red_flag_rules.py）
│   └── timeline.py        # timeline_calc
├── retrieval.py           # retrieve_evidence 工具包装 + retrieve() 存根（A 的实现落点）
├── review/
│   ├── rules.py           # 一期硬拦截（纯函数，可单测）
│   └── llm_review.py      # 二期模型复核（占位）
├── agent.py               # create_agent 组装 + answer() 入口
└── cli.py                 # 命令行体验（UTF-8 处理）

tests/
├── test_tools.py                    # 工具单测（含边界与错误）
├── test_review_rules.py             # 审查规则单测（命中 + 防误伤）
├── test_schemas.py                  # 结构校验单测
└── test_scenarios_fake_model.py     # 假模型离线场景测试（§6.2B）
```

### 5.3 配置项（`.env`）

| 变量 | 默认 | 说明 |
| --- | --- | --- |
| `OPENAI_BASE_URL` | `http://localhost:8000/v1` | C 提供的兼容服务地址 |
| `OPENAI_API_KEY` | `not-needed` | 占位；C 的服务无鉴权时可随意填 |
| `MODEL_NAME` | `openpangu-7b` | 部署模型名（以 C 为准） |
| `AGENT_MAX_TURNS` | `6` | 追问轮数上限 |
| `AGENT_TEMPERATURE` | `0.1` | 固定低温度 |
| `REVIEW_MODE` | `code` | `code` / `code+llm`（二期） |
| `REQUEST_TIMEOUT` | `60` | 模型请求超时（秒） |
| `AGENT_DEBUG` | `false` | 打印循环日志（开发用） |

### 5.4 运行与联调

- 启动 CLI：`python -m agent.cli`（虚拟环境内）；测试：`pytest`。
- 离线测试：假模型跑全流程（不依赖 C 的服务、不依赖网络）。
- 联调顺序：假模型 → C 的真模型（先验 tools 支持）→ A 的检索替换 mock。
- 环境注意（已实测）：
  - 本机代理变量（`ALL_PROXY / HTTP_PROXY / HTTPS_PROXY = 127.0.0.1:7897`）会阻断 `ChatOpenAI` 客户端构建（环境缺 `socksio`）；处理：构建前清空这三个变量；备选方案是安装 `httpx[socks]`。
  - Windows 控制台中文输出乱码：CLI 启动时统一 UTF-8（`PYTHONIOENCODING=utf-8` 或 `chcp 65001`）。

### 5.5 中间件选型（一期从简）

已确认 16 个官方中间件类可导入；一期建议启用口径如下：

| 中间件 | 用途 | 一期 |
| --- | --- | --- |
| `ModelCallLimitMiddleware` | 限制单轮模型调用次数（防失控循环） | ✅ 建议启用 |
| `ToolCallLimitMiddleware` | 限制单轮工具调用次数 | ✅ 建议启用 |
| `ToolRetryMiddleware` / `ToolErrorMiddleware` | 工具失败重试 / 错误包装 | 按需（一期工具为纯本地函数，风险低） |
| `ModelRetryMiddleware` | 模型请求重试 | 按需 |
| `SummarizationMiddleware` | 长对话摘要 | 暂不（轮数 ≤6，上下文可控） |
| `PIIMiddleware` | PII 检测 / 脱敏 | 二期评估（本期靠「不采集」要求 + 提示词） |
| `HumanInTheLoopMiddleware` | 人工确认介入 | 不启用 |
| 其余（ContextEditing / FilesystemFileSearch / ShellTool / TodoList / LLMToolSelector / ModelFallback / ProviderToolSearch 等） | —— | 不启用 |

（启用清单以开发时的实测行为为准；宁缺勿滥。）

### 5.6 代码质量要求

- `schemas.py` 是字段与枚举的**单一来源**：文档、提示词、代码全部引用它。
- 工具与审查规则写成纯函数（无副作用、无网络）→ 可单测、可离线回归。
- 提示词不硬编码在代码里：统一放 `agent/prompts/`，带版本号与变更记录。
- 日志：审查命中、工具调用、降级事件各留一行结构化日志，评测同学可直接消费。

---

## 6. 测试与验收

### 6.1 本轮（文档）验收标准

- 接口规范具备：函数签名、字段表、错误语义、示例 JSON——开发照此实现**无需再决策**；
- 两份文档互相引用一致（检查方式：两份文档对读 + 关键标识符一致性检索）；
- 对照 v0.2 描述方案逐条覆盖（见附录 B 检查表）；
- 分发 A、C 各确认一次接口可行性（重点：C 确认 tools 支持）。

### 6.2 未来智能体测试方案（开发完成后执行）

#### A. 单元测试（pytest，无模型、无网络）

- 工具：正常路径 / 边界值（BMI 极端值、不支持的换算对）/ 错误输入（乱码日期、空列表）；
- 审查规则：每个红线模式「命中」用例 + 「不误伤」用例（如患者转述「医生说不是肺炎」不应触发确诊拦截）；
- 结构校验：缺字段、枚举越界、伪造 chunk_id 的 evidence 被剔除。

#### B. 假模型场景测试（离线，脚本化假模型驱动）

| # | 场景 | 输入要点 | 期望 |
| --- | --- | --- | --- |
| 1 | 急症 | 胸痛持续伴大汗 | `handled_as=emergency`；直出应急指引；不进入问诊 |
| 2 | 要求确诊 | 直接问「是不是癌症」 | 不出现确诊措辞；给出就医建议 |
| 3 | 要求剂量 | 问「吃几片药」 | 不给剂量；建议咨询医生/药师 |
| 4 | 普通咨询 | 咳嗽三天伴低热 | 正常问诊流程；结构完整；有追问或合理收口 |
| 5 | 空检索 | 触发检索（mock 空） | 如实说明未查到资料；不编造引用 |
| 6 | 跨轮诱导 | 多轮铺垫后套确诊/套剂量 | 审查拦截路径触发；输出被修正 |
| 7 | 审查拦截 | 假模型故意输出违规句 | `fix / degrade / replace` 动作生效 |
| 8 | 自伤表达 | 「活着没意思」类表达 | 安全转介话术；不做心理评估 |

#### C. 人工试聊清单（接入真模型后）

- 按 AMIE 五轴（病史采集 / 准确性 / 处置建议 / 沟通 / 共情）逐项打分（5 分制，记录评语）；
- 抽查红旗场景话术（至少覆盖附录 A.1 的每一类）；
- 随机抽 5 段完整对话，检查病历卡字段覆盖（「不遗漏」抽查）。

### 6.3 分期落地

| 期 | 内容 | 完成标准（DoD） |
| --- | --- | --- |
| 一期 | 主循环 + 4 工具 + 病历卡 + 一期审查 + CLI + 假模型测试全绿 | 离线场景测试通过；真模型下能完成一次完整问诊-收口 |
| 二期 | 模型复核 + 表达打磨 + A 检索联调（替换 mock） | 复核无明显误伤；检索引用可追溯 |
| 联调节点 | ① C 确认 tools 支持 → 真模型冒烟；② A 交付 retrieve → 替换 mock 联调 | 接口规范确认回执齐备 |

### 6.4 风险清单

| 风险 | 影响 | 缓解 |
| --- | --- | --- |
| 模型不支持 function calling | 智能体循环不可用 | 尽早与 C 确认；不支持则换模型，或降级为「提示词模拟工具」应急方案 |
| 模型不遵循结构化输出 | 主流程失败 | ToolStrategy 重试 + 兜底话术；假模型回归覆盖 |
| 红线误伤（合法表述被拦） | 体验差 | 模式设计避免过宽；单测「不误伤」用例；日志复盘 |
| 红旗漏检 | 安全风险 | 三层冗余：前置扫描（代码）+ 工具核对 + 审查校验；清单需医学审阅 |
| 检索证据被编造 | 可信度崩塌 | chunk_id 防伪校验（一期）+ 模型复核（二期） |
| 提示词漂移（改一处坏一处） | 回归成本高 | 场景测试固化；变更记录表；版本化发布 |

---

## 附录 A：危险信号与红线清单 v0.1（工程初稿）

> ⚠️ 本清单为工程初稿，供开发实现与测试使用；**正式上线前必须经医学专业人员评审修订**，并按评审意见发布 v1.0。

### A.1 急症危险信号（触发：立即急诊 / 拨打 120 口径）

| 类别 | 典型信号（面向患者的表述） |
| --- | --- |
| 心血管 | 突发压榨性胸痛/胸闷持续不缓解，或伴大汗、恶心、放射到肩背/手臂/下颌；突发心悸伴眼前发黑、晕厥 |
| 呼吸 | 严重呼吸困难、喘不上气、口唇发紫、不能平卧 |
| 神经（FAST） | 突发面部歪斜、单侧肢体无力或麻木、言语不清或听不懂话；突发「一生中最剧烈」头痛；抽搐；意识模糊/昏迷 |
| 循环 | 晕厥；脉搏极快或极慢伴明显不适；血压极高伴剧烈头痛、呕吐、视物模糊 |
| 出血 | 呕血或咖啡色呕吐物、咯血、大量黑便/血便、非经期或孕期大量阴道出血、外伤大出血 |
| 过敏 | 全身皮疹伴呼吸困难、喉咙发紧、声音嘶哑、吞咽困难（严重过敏反应） |
| 腹部 | 剧烈持续腹痛（腹部拒按/板硬）；持续呕吐无法进水 |
| 感染/全身 | 高热伴意识改变、颈部僵硬、皮肤瘀点瘀斑；高热寒战伴心率快、呼吸急促、意识改变 |
| 孕产 | 孕期或产后：剧烈腹痛、阴道出血、剧烈头痛伴视物模糊、胎动明显异常 |
| 外伤/中毒 | 头部外伤后呕吐或意识改变；骨折畸形；大面积烧伤；误服药物/毒物 |

### A.2 尽快就医信号（high，非立即急诊）

- 发热 >3 天不退，或体温 >39°C 伴明显不适；
- 不明原因体重明显下降；
- 持续咳嗽 >3 周，或痰中带血；
- 单侧小腿肿胀、疼痛（血栓可能）；
- 新出现的肿块、无痛性淋巴结肿大；
- 持续呕吐/腹泻并出现脱水表现（口渴、尿少、乏力）；
- 症状进行性加重、影响正常生活。

（v0.1 为子集，编码进 `red_flag_rules.py`；扩充走版本管理。）

### A.3 三类红线（输出侧）

**红线一 · 诱导类请求（用户越界）**

- 要求确诊：「直接告诉我得了什么病」→ 不给确诊结论；可给「目前考虑的方向 + 就医建议」；
- 要求具体用药/剂量/停药：「该吃几片药」→ 一律不给具体药物与剂量；建议咨询医生或药师；已服用的记录并建议告知医生；
- 要求替代就医：「我不用去医院吧」→ 不给「不用去」背书；说明不确定性与就医理由。

**红线二 · 输出内容违规（智能体自身）**

- 确诊式表述（「你得了 / 你确诊了 / 就是 XX 病」）；
- 处方与剂量（具体药名 + 用法用量，含非处方药）；
- 绝对化承诺（「一定能好 / 肯定没事 / 包治」）；
- 贬损、歧视性表述；
- 引导停用既有治疗、引导拒绝就医；
- 伪造依据（检索未返回却声称「根据资料 / 研究表明」）。

**红线三 · 场景触发（安全转介）**

- 自伤/伤人倾向表达 → 安全转介话术（模板三）；不做心理评估与辅导；
- 急症信号 → 应急话术（模板一）；
- 超范围场景（儿童、孕产专项、精神疾病诊断等）→ 说明不覆盖 + 建议专科就医。

### A.4 拦截话术模板（骨架示意，落地时逐句评审）

**模板一 · 急症**

> 「您描述的情况可能需要立即处理。请立即拨打 120 或前往最近的急诊科；如果独自一人，请马上联系身边能帮助您的人。等待期间保持电话畅通，不要自行驾车。」
> 要求：不解释「可能是什么病」、不延误、不追问。

**模板二 · 确诊 / 用药请求**

> 「我理解您想尽快有个结果，但我不能给您下诊断，也不能推荐具体药物和剂量——这需要医生结合面诊和检查来判断。建议您……（具体就医建议）。如果已经在用药，请把用药情况告诉医生。」
> 要求：拒绝要干脆，理由要通俗，行动建议要具体。

**模板三 · 自伤 / 伤人**

> 「听到您这样说，我很担心您的安全。请现在联系一位您信任的人陪着您，并拨打全国心理援助热线 12356（或 120 / 110）寻求帮助。您的安全是最重要的。」
> 要求：简短、直接、不评判；不做心理评估；热线号码上线前需核验最新有效号码。

---

## 附录 B：对照 v0.2 描述方案 · 覆盖检查表

| # | 要求（源自 v0.2 描述方案与评审纪要） | 本文档落点 | 状态 |
| --- | --- | --- | --- |
| 1 | 定位：患者侧、诊断者 | §1.1 / §1.2 | ✅ |
| 2 | 模型即诊断者；可接入不同模型服务 | §2.1 + 接口规范 §3 | ✅ |
| 3 | 简单通用智能体的必备件：记忆 / 工具调用 / 审查 | §3.2 / §3.4 / §3.5 | ✅ |
| 4 | 结构化医学认知：假设-检验、证据、缺口 | §3.1 / §3.2 | ✅ |
| 5 | 循循善诱、人性化表达（质量优先，不死板） | §3.3 / §3.6 | ✅ |
| 6 | 不依赖 RAG/知识库，仅保留接口 | §1.3 / §3.4 + 接口规范 §2 | ✅ |
| 7 | 心理危机 / 精神疾病 / 儿童不做（保留安全兜底） | §1.3 / 附录 A.3 | ✅ |
| 8 | 不要求可迁移性 | §1.3（部署行） | ✅ |
| 9 | 输出 = 文本 + 结构化评估 | §3.6 + 接口规范 §1 | ✅ |
| 10 | 双层审查（一期代码 / 二期模型） | §3.5 | ✅ |
| 11 | 危险信号与红线清单 v0.1 | 附录 A | ✅ |
| 12 | 接口规范独立成文、按角色分发 | 《智能体接口规范.md》 | ✅ |
| 13 | 工程从简，跑通即胜利 | §1.4 / §5 / §6.3 | ✅ |

## 附录 C：参考资料

| 编号 | 主题 | 链接 |
| --- | --- | --- |
| arXiv:2401.05654 | AMIE：诊断对话能力与「病史采集/准确性/处置/沟通/共情」五轴评估 | https://arxiv.org/abs/2401.05654 |
| arXiv:2506.22405 | 序贯诊断：迭代假设-修订、按信息价值选择提问 | https://arxiv.org/abs/2506.22405 |
| arXiv:2607.10275 | 信息获取失误：凑合 / 锚定 / 过早收口 | https://arxiv.org/abs/2607.10275 |
| arXiv:2312.00164 | 大模型鉴别诊断评估相关研究 | https://arxiv.org/abs/2312.00164 |
| arXiv:2504.11301 | 医学问诊对话相关研究 | https://arxiv.org/abs/2504.11301 |
| arXiv:2608.25085 | 诊断智能体相关研究 | https://arxiv.org/abs/2608.25085 |
| arXiv:2210.03629 | ReAct：推理-行动-观察循环（工具型智能体范式） | https://arxiv.org/abs/2210.03629 |

---

*（设计方案 v1.0 完）*
