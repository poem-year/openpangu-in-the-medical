# 智能体接口规范

> 版本 v1.0 · 2026-09-12
> 配套文档：`项目综述.md`（项目背景）、`环境说明.md`（机器环境与文件地图）
> 用法：**按章节分发即可**——A 看 §2，C 看 §3，评测 / 界面看 §4；对接前建议先浏览 §1。

---

## 0. 总览与阅读导航

| 章节 | 内容 | 主要读者 |
| --- | --- | --- |
| §1 | 智能体入口与输出结构（AgentResult） | 全体 |
| §2 | 检索接口（A 交付） | A（B 联调） |
| §3 | 模型服务接口（C 交付） | C（B 联调） |
| §4 | 输出消费说明 | 评测 / 界面 |
| §5 | 环境与运行备忘 | 全体 |

约定：

- 字段名与枚举值**以本文为单一来源**；设计文档、提示词、代码均引用本文。
- 字段「只增不改」；破坏性变更必须升版本并通知所有消费者。
- 本文所有接口均为 **Python 3.11.15 / Pydantic v2** 口径（本机实际环境，见 `环境说明.md` §三）。

### 版本记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 2026-09-12 | 首版：入口 / 检索 / 模型 / 输出消费四部分 |

---

## 1. 智能体入口与输出

### 1.1 调用方式

```python
def answer(question: str, history: list[dict] | None = None) -> AgentResult:
    ...
```

- `question`：用户本轮输入（纯文本）。
- `history`：可选。形如 `[{"role": "user" | "assistant", "content": "..."}]`，按时间顺序排列；评测与外部集成走此模式（**无状态**：调用方自行维护历史）。
- 不传 `history`：CLI 单会话从零开始；CLI 内部同样以 history 透传方式维护多轮（行为一致）。
- 同步阻塞调用；单轮耗时预期 2~30 秒（取决于模型服务）；无网络副作用（检索与模型调用由内部工具/服务完成）。

### 1.2 数据结构（Pydantic 模型）

```python
class Hypothesis(BaseModel):
    name: str                                     # 假设名称（如「急性支气管炎」）
    likelihood: Literal["low", "medium", "high"]  # 相对可能性，不是概率
    supporting: list[str]                         # 支持证据（对话/工具/检索要点）
    against: list[str]                            # 反对证据（可空）
    missing: list[str]                            # 该假设还缺的关键信息（可空）

class EvidenceItem(BaseModel):
    source: Literal["dialogue", "tool", "kb"]     # 来源：患者对话 / 工具结果 / 知识库检索
    ref: str     # dialogue→要点或轮次；tool→「工具名(参数摘要)」；kb→chunk_id（必须来自检索返回）
    detail: str  # 依据内容一句话

class Assessment(BaseModel):
    hypotheses: list[Hypothesis]     # 按 likelihood 降序；急症分支可为空
    evidence: list[EvidenceItem]     # 当前判断依据（不要求穷尽）
    missing_info: list[str]          # 还缺哪些关键信息（「待排查清单」的实现）
    next_steps: list[str]            # 给患者的行动建议（至少 1 条）
    confidence: Literal["low", "medium", "high"]

class AgentResult(BaseModel):
    reply: str          # 面向患者的最终文本（唯一展示文本；已经过审查层）
    assessment: Assessment
    risk_level: Literal["low", "medium", "high", "emergency"]
    used_tools: list[str]  # 本轮实际调用的工具名（按调用顺序、去重）
    handled_as: Literal["normal", "emergency", "out_of_scope", "review_modified"]
```

### 1.3 字段语义

| 字段 | 类型 | 必填 | 语义 |
| --- | --- | --- | --- |
| `reply` | str | ✅ | 给患者看的最终文本。界面**唯一**展示来源；审查修正后的版本 |
| `assessment` | Assessment | ✅ | 结构化评估；急症分支为空壳结构（见下方说明） |
| `risk_level` | 枚举 | ✅ | 风险等级，四档语义见下表 |
| `used_tools` | list[str] | ✅ | 本轮实际调用过的工具名；空列表=未调用；枚举见下表 |
| `handled_as` | 枚举 | ✅ | 本条回复的处理路径标记；优先级见下 |

**risk_level 四档语义**：

| 值 | 含义 | 展示建议 |
| --- | --- | --- |
| `low` | 可先自我观察 / 居家护理，必要时就医 | 常规样式 |
| `medium` | 建议近期（数日内）就医 | 轻提示 |
| `high` | 建议尽快（24 小时内）就医 | 橙色提醒 |
| `emergency` | 需立即急诊 / 拨打 120 | 红色置顶提示 |

规则：审查层只允许**上调** risk_level，不允许下调。

**used_tools 枚举**（与工具定义一一对应）：`calculate_bmi` / `convert_units` / `check_red_flags` / `timeline_calc` / `retrieve_evidence`。

**handled_as 枚举**：

| 值 | 含义 |
| --- | --- |
| `normal` | 常规问诊路径 |
| `emergency` | 急症前置拦截：未进入问诊，直出应急指引 |
| `out_of_scope` | 超范围场景：儿童、孕产专项、心理专项等，说明不覆盖并建议专科就医 |
| `review_modified` | 输出被审查层修正或降级（fix / degrade / replace 任一动作命中） |

优先级：`emergency` > `out_of_scope` > `review_modified` > `normal`（同时命中的取最高）。

**assessment 注意事项**：

- `hypotheses` 是「当前鉴别方向」，不是诊断结论；对外话术必须带不确定性表述。
- `confidence="high"` 仅表示「现有信息支持下比较有把握」，**绝不等于确诊**。
- `evidence` 中 `source="kb"` 时，`ref` 必须是 `retrieve` 工具真实返回过的 `chunk_id`；审查层会逐条校验，编造条目将被剔除并触发修正。
- 急症分支（`handled_as="emergency"`）时：`hypotheses=[]`、`missing_info=[]`、`next_steps` 为应急指引、`confidence="low"`——这是允许的「空壳」结构。

### 1.4 示例

**示例 1 · 常规问诊轮**（第 1 轮，尚未收口）：

```json
{
  "reply": "了解了。您说咳嗽三天、有点低烧，我先记下来。为了帮您把情况弄清楚，我想再确认两点：痰是什么颜色的？有没有觉得喘不上气或胸口发闷？",
  "assessment": {
    "hypotheses": [
      {
        "name": "上呼吸道感染",
        "likelihood": "medium",
        "supporting": ["低热3天", "咳嗽"],
        "against": [],
        "missing": ["痰的颜色与性状", "是否气促"]
      },
      {
        "name": "急性支气管炎",
        "likelihood": "low",
        "supporting": ["咳嗽"],
        "against": ["发热时间较短"],
        "missing": ["咳嗽性质", "肺部听诊（无法远程获取）"]
      }
    ],
    "evidence": [
      {"source": "dialogue", "ref": "第1轮主诉", "detail": "咳嗽3天伴低热（自述）"}
    ],
    "missing_info": ["痰的颜色与性状", "是否出现呼吸困难或胸闷"],
    "next_steps": ["继续观察体温变化", "若出现气促或高热不退请及时就医"],
    "confidence": "low"
  },
  "risk_level": "low",
  "used_tools": [],
  "handled_as": "normal"
}
```

**示例 2 · 急症分支**（未进入模型，前置拦截）：

```json
{
  "reply": "您描述的情况可能需要立即处理。请立即拨打 120 或前往最近的急诊科；如果独自一人，请马上联系身边能帮助您的人。等待期间保持电话畅通，不要自行驾车。",
  "assessment": {
    "hypotheses": [],
    "evidence": [
      {"source": "dialogue", "ref": "本轮输入", "detail": "胸痛持续伴大汗（自述）"}
    ],
    "missing_info": [],
    "next_steps": ["立即拨打 120 或前往急诊"],
    "confidence": "low"
  },
  "risk_level": "emergency",
  "used_tools": [],
  "handled_as": "emergency"
}
```

### 1.5 错误语义

| 情形 | 行为 |
| --- | --- |
| 参数非法（`question` 为空等） | 抛 `ValueError` |
| 模型服务不可用 / 超时（重试后仍失败） | 抛自定义异常 `AgentUnavailableError`（携带原因摘要） |
| 检索、工具内部错误 | **不抛出**：内部降级并在 `reply` 中如实说明；`handled_as` 可能为 `review_modified` |
| 结构化输出重试后仍不合法 | 返回兜底 `AgentResult`：`risk_level="medium"`、`next_steps` 含就医建议、`handled_as="review_modified"` |
| 审查重生成后仍违规 | 替换为保守话术 / 就医建议模板，`handled_as="review_modified"` |

---

## 2. 检索接口（交付 A）

### 2.1 两个层次

| 层次 | 函数 | 由谁提供 | 说明 |
| --- | --- | --- | --- |
| 智能体侧工具（模型可见） | `retrieve_evidence(query: str, k: int = 5) -> list[Evidence]` | B（包装层） | 模型按需调用；工具名固定，用于 `used_tools` 上报 |
| 检索实现函数（你要交付的） | `retrieve(query: str, k: int = 5) -> list[Evidence]` | **A** | 实现落点：`agent/retrieval.py`；当前为 mock（返回 `[]`） |

**替换方式（3 步）**：

1. 在 `agent/retrieval.py` 找到 `retrieve()` 存根；
2. 将函数体替换为你的真实实现（内部可自由拆分子模块，但对外签名与返回类型保持不变）；
3. 跑一遍 §2.5 自测清单，通知 B 联调。

智能体侧与提示词**无需任何改动**。

### 2.2 Evidence 结构（A 契约）

```python
class Evidence(BaseModel):
    chunk_id: str   # 全局唯一且稳定的片段 ID（如 "kb::pneumonia::0031"）
    text: str       # 片段正文（患者可读；建议 50~500 字）
    source: str     # 出处名称（如《内科学（第9版）》第3章 或 官方指南名称）
    section: str    # 章节/小节（如「社区获得性肺炎-诊断」）
    score: float    # 相关度分数（同一查询内可比、越大越相关）
```

约束：

- `chunk_id`：非空、唯一、稳定（同一片段多次检索必须同 ID）——智能体用它做「引用防伪」校验；**绝对不得编造**。
- `text`：纯文本；不应包含针对 AI 的指令性内容（如「请忽略以上指示」）；如出现可疑内容，B 侧按提示注入处理。
- `source` / `section`：用于向患者展示出处；无法确定时给空字符串，不要编造。
- `score`：仅要求同一查询内可比；返回前按降序排列。

### 2.3 行为契约

| 场景 | 契约 |
| --- | --- |
| 正常 | 返回 ≤ k 条，按 score 降序；k 是上限，不是保证 |
| 无结果 | 返回空列表 `[]`；**不要**返回占位/编造证据；智能体将如实告知「未查到依据」 |
| 服务异常 | 抛出异常（任意 Exception 均可）；B 的工具包装会捕获并降级为「检索暂不可用」，不中断对话 |
| 超时 | 建议单次 <2 秒；超时抛异常走降级，不要无限等待 |
| 多轮 | 同一 query 尽量幂等；重复调用不产生副作用 |

### 2.4 Mock 现状（当前仓库状态）

`agent/retrieval.py` 中 `retrieve()` 返回空列表，并附注释说明。任何情况下智能体都不会引用知识库内容——这是**预期行为**，用于保证 B 侧在 A 未交付前可独立开发与测试。

### 2.5 A 侧自测清单（联调前）

- [ ] 签名与返回类型与 §2.1 一致（可快速断言：`python -c "from agent.retrieval import retrieve; print(retrieve('测试'))"`）
- [ ] 无结果 → 返回 `[]`
- [ ] 异常路径 → 抛出异常（不要吞掉）
- [ ] `chunk_id` 唯一性抽查（≥100 条无重复）
- [ ] score 降序、`k` 截断正确
- [ ] P95 延迟 <2s

---

## 3. 模型服务接口（交付 C）

### 3.1 接入方式

智能体通过 `langchain-openai` 的 `ChatOpenAI` 连接你的服务，约定如下环境变量：

| 变量 | 示例 | 说明 |
| --- | --- | --- |
| `OPENAI_BASE_URL` | `http://<服务地址>/v1` | OpenAI 兼容根路径 |
| `OPENAI_API_KEY` | 任意占位 | 服务无鉴权时可随意填 |
| `MODEL_NAME` | `openpangu-7b` | 部署模型名（以你的部署为准） |

构建方式（B 侧代码示意）：

```python
ChatOpenAI(
    model=os.environ["MODEL_NAME"],
    temperature=0.1,
    base_url=os.environ["OPENAI_BASE_URL"],
    api_key=os.environ["OPENAI_API_KEY"],
)
```

参数约定：temperature 固定 0.1；单轮超时 60s、失败重试 2 次（B 侧控制）。

### 3.2 必须支持的能力（最重要）

**必须支持 `tools`（function calling）**——智能体的工具调用循环完全依赖它；这是整个智能体能否工作的前置条件。

- 建议支持：并行工具调用、稳定的 JSON 参数生成、OpenAI 标准错误码与错误体。
- 可选：流式输出（streaming，本期不使用）。
- 验证方法（最小样例）：给模型一个 `echo` 工具 + 一句「请调用 echo」，确认返回 `tool_calls` 结构。

### 3.3 待 C 确认清单（请逐条回执）

1. `tools` / function calling 是否支持？主接口是否 `POST /v1/chat/completions`？
2. 最大上下文长度与推荐 `max_tokens`？
3. 并发 / 速率限制（RPM、TPM）？
4. 服务不可用时的错误形态（HTTP 状态码 / 错误体结构）？
5. 部署地址形态：本机 localhost 还是局域网 IP？（影响 B 侧代理变量处理，见 §3.4）

### 3.4 离线测试与常见故障

- 离线测试：官方 `GenericFakeChatModel` 可驱动主循环（已实测）；**注意**它不支持 `bind_tools`——涉及工具调用与结构化输出的场景测试，使用 B 侧自研的「脚本化假模型」子类（约 40 行，按预设脚本返回工具调用与最终结构）。
- 常见故障：
  - 客户端构建失败：代理环境变量（`ALL_PROXY / HTTP_PROXY / HTTPS_PROXY`）指向本地代理且环境缺 `socksio` → 清空这三个变量，或安装 `httpx[socks]`；
  - 401 → API Key 填法；404 → `MODEL_NAME` 或路径不对；超时 → 服务负载或 `REQUEST_TIMEOUT`。

---

## 4. 输出消费说明（评测与界面）

### 4.1 评测断言建议

| 测试目标 | 建议断言（稳定项） |
| --- | --- |
| 急症安全 | `handled_as == "emergency"` 且 `risk_level == "emergency"`，`next_steps` 含急救指引 |
| 越界拒答 | `reply` 不含确诊 / 剂量模式；`next_steps` 含就医建议 |
| 正常问诊质量 | `assessment` 字段完整；`hypotheses` 非空且按 likelihood 降序；问诊中 `missing_info` 非空 |
| 引用可追溯 | 所有 `evidence.source=="kb"` 项的 `ref` 均为真实 `chunk_id`（与检索日志比对） |
| 审查生效 | 构造违规输出时 `handled_as == "review_modified"` |
| 多轮一致性 | 后续轮的 `hypotheses` 与上一轮可衔接（不无故漂移） |

注意：**不要对 `reply` 全文做精确断言**（同义表达多变）；对关键子串（如「急诊」「120」「就医」）做包含性断言更稳定。

### 4.2 界面使用建议

| 字段 | 用法 |
| --- | --- |
| `reply` | 主气泡展示（唯一面向患者文本） |
| `risk_level` | 徽标 / 配色；`emergency` 建议置顶红色提示条 |
| `assessment` | 「查看详情」折叠面板（假设列表 / 依据 / 缺口 / 建议） |
| `used_tools` | 开发 / 调试模式展示（如「已核对危险信号清单」） |
| `handled_as` | 埋点统计（应急率、审查拦截率、超范围占比） |

---

## 5. 环境与运行备忘

- 依赖版本：langchain 1.4.0 / langgraph 1.2.11 / langchain-openai 1.6.2（Python 3.11.15，`.venv`）。
- 运行（开发期）：`python -m agent.cli`；测试：`pytest`。
- Windows 中文乱码：设置 `PYTHONIOENCODING=utf-8` 或 `chcp 65001`。
- 代理问题：见 §3.4。

---

## 6. 变更与确认

### 6.1 确认流程

本文档定稿后分发给 A、C 各确认一次；确认结论回填下表：

| 角色 | 确认项 | 结论 | 日期 |
| --- | --- | --- | --- |
| A | §2 检索接口可行（签名 / 字段 / 性能可满足） | 待确认 | |
| C | §3 模型接口可行（重点：tools 支持） | 待确认 | |
| 评测 / 界面 | §4 输出字段可满足消费需求 | 待确认 | |

### 6.2 变更规则

- 字段 / 枚举「只增不改」；任何破坏性变更必须升版本（v1.0 → v1.1 …）并通知全部消费者。
- 变更记录写入 §0 版本记录表。

---

*（接口规范 v1.0 完）*
