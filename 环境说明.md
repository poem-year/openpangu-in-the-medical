# 环境说明 · openPangu 医疗诊断能力增强

> 更新：2026-09-13 ｜ 读者：接手本项目的 AI agent
> 配套：`项目综述.md`（我们在做什么）· `智能体接口规范.md`（接口契约）· `模型部署说明.md`（模型怎么跑）

本文只回答一件事：**在这台机器上干活需要知道什么**。看完本文 + `项目综述.md` 就能开工；
动手改代码前再看 `AGENTS.md` 的工作约定。

---

## 一、这台机器（实测）

| 项 | 值 |
| --- | --- |
| 平台 | 模力方舟（moark.com）算力容器，按小时计费，用 SSH / VS Code Remote-SSH 连接 |
| 架构 / 内存 | x86_64，128 核，64GB |
| NPU | 昇腾 910B2C × 1，64GB HBM（`npu-smi info` 查看，健康状态 OK） |
| 系统 | openEuler 24.03 LTS-SP2 |
| Python | 3.11.15（`/usr/local/python3.11.15/bin/python3`），另有 `/opt/conda` 可选 |
| 磁盘 | `/` 30G、`/data` 59G —— **大文件一律放 `/data`** |
| 容器自带 CANN | 9.0.0（`/usr/local/Ascend/cann-9.0.0`）——**缺 910B 算子内核，跑不了模型** |
| 实际使用的 CANN | 8.3.RC1（`/data/Ascend/ascend-toolkit/latest`，另装的一套，12GB） |
| 没有 | Docker / podman / buildah（容器内也装不上，服务只能裸装） |
| 持久化 | `/root`、`/data` 跨镜像保留；实例销毁则全清；关机保 36 小时 |
| 网络 | 可访问外网（DeepSeek、pip 源等）；平台禁用 `scp`；并发 SSH 会话数受限 |

为什么不用容器自带的 CANN 9.0.0、为什么不用 vLLM，见 `模型部署说明.md` §二。
**不要自己另选部署版本组合**，可用的组合只有 `模型部署说明.md` 里那几套。

## 二、工作目录

| 路径 | 内容 |
| --- | --- |
| `/data/openpangu` | 项目根目录：文档、代码、测评集、评测脚本 |
| `/data/openpangu/.venv` | B 线智能体环境（Python 3.11.15，见 §三） |
| `/data/openpangu/.venv-pangu` | 模型推理环境（Python 3.11.15，见 §三） |
| `/data/models/openPangu-R-7B-2512` | 模型权重，17GB，4 个 safetensors 分片 |
| `/data/Ascend` | 另装的 CANN 8.3.RC1 |
| `/data/huggingface_home` | 平台预置的 HuggingFace 缓存（BGE-M3 已缓存在此） |
| `/root/.codex` | Codex 配置（DeepSeek 接入、API Key、备份） |

## 三、两个 Python 环境（不要混用）

| 环境 | 解释器 | 装了什么 | 用来做什么 |
| --- | --- | --- | --- |
| `.venv` | `.venv/bin/python` | langchain 1.4.0、langgraph 1.2.11、langchain-openai 1.6.2、pydantic、pytest | 跑 `agent/` 与 `tests/` |
| `.venv-pangu` | `.venv-pangu/bin/python` | torch 2.7.1+cpu、torch-npu 2.7.1、transformers 4.53.2 | 跑模型推理与 `eval/` |

- `.venv` 的精确依赖钉在 `requirements.txt`，在 Python 3.11.15 上实测全部可装。
- 两套环境互不影响：**智能体与单测走 `.venv`，模型推理与评测走 `.venv-pangu`**。
- 两套都是 Python 3.11.15（文档里若出现 3.13 的说法已过期）。

## 四、各文件的功能

### 4.1 根目录文档

| 文件 | 作用 |
| --- | --- |
| `项目综述.md` | 项目一页纸：做什么、怎么做、谁做、怎么证明。**开工前先读** |
| `环境说明.md` | 本文：机器、环境、文件地图、命令、已知坑 |
| `智能体接口规范.md` | 三条线之间的接口契约（函数签名、字段、枚举、错误语义） |
| `模型部署说明.md` | openPangu 怎么跑、实测速度、批处理、部署坑 |
| `AGENTS.md` | 这台机器上的工作约定（测试门槛、提示词变更流程、不要做的事） |

### 4.2 B 线 · 智能体（`agent/`）

| 文件 | 功能 |
| --- | --- |
| `agent/agent.py` | 智能体组装与对外入口 `answer(question, history) -> AgentResult`：前置安全扫描 → 主循环 → 审查 → 输出 |
| `agent/cli.py` | 命令行聊天入口（`python -m agent.cli`，支持 /new、/debug、/json） |
| `agent/config.py` | 配置读取：环境变量 + `.env`，默认值见 §六 |
| `agent/schemas.py` | 全部 Pydantic 数据结构（`AgentResult` / `AgentTurnOutput` / `Assessment` / `CaseCard` / `Evidence`），字段与枚举的单一来源 |
| `agent/errors.py` | 异常定义（`AgentError` / `AgentUnavailableError`） |
| `agent/prompt_loader.py` | 提示词与固定话术加载 |
| `agent/recorder.py` | 单轮工具调用记录：供 `used_tools` 输出与「引用防伪」校验 |
| `agent/retrieval.py` | 检索工具 `retrieve_evidence`（模型可见）+ `retrieve()` 实现落点；**当前 `retrieve()` 仍返回 `[]`（mock）** |
| `agent/tools/` | 4 个本地工具：`bmi.py` / `units.py` / `red_flags.py` / `timeline.py`；`red_flag_rules.py` 是危险信号规则数据（单一来源，同时服务工具与前置扫描） |
| `agent/review/rules.py` | 一期硬拦截规则（纯函数、可单测）：确诊措辞 / 用药剂量 / 绝对化承诺 / 引用防伪 / 红旗一致性 / 收口检查 |
| `agent/review/engine.py` | 审查流程：检测 → 修正重生成 → 代码级降级 → 兜底替换 |
| `agent/review/llm_review.py` | 二期模型复核（占位，未启用） |
| `agent/prompts/` | 版本化提示词：`system_prompt_v1.md`、`review_fix_v1.md`、`emergency_templates_v1.md`、`CHANGELOG.md` |

### 4.3 B 线 · 离线测试（`tests/`）

83 项，全部不需要模型、不需要联网。

| 文件 | 覆盖内容 |
| --- | --- |
| `tests/test_tools.py` | 4 个工具的正常路径 / 边界值 / 错误输入，前置安全扫描 |
| `tests/test_review_rules.py` | 每条红线模式的「命中」与「不误伤」用例，审查动作 |
| `tests/test_schemas.py` | 结构校验：缺字段、枚举越界 |
| `tests/test_scenarios_fake_model.py` | 假模型驱动的 8 大离线场景（急症 / 套确诊 / 套剂量 / 普通问诊 / 空检索 / 跨轮诱导 / 拦截降级 / 自伤） |
| `tests/fake_model.py` | 脚本化假模型（`BaseChatModel` 子类，支持 `bind_tools`） |
| `tests/conftest.py` | pytest 路径配置 |

### 4.4 A 线 · 知识库与检索（`rag_med_project/`）

| 文件 | 功能 |
| --- | --- |
| `rag_med_project/retrieval.py` | BGE-M3 向量检索 + Chroma 持久化库，`retrieve(query, k)` 返回带出处的片段 |
| `rag_med_project/knowledge_base/docs` | 原始医学文档（目前只有 2 个测试文档，真实语料未入库） |
| `rag_med_project/knowledge_base/chroma_db` | Chroma 向量库 |

注意：A 线的 `retrieve()` 返回 `dict{query, evidence, msg}`，与 `智能体接口规范.md` §2 约定的
`list[Evidence]` 不一致，score 语义也相反（A 侧是距离，越小越相似）。B 线尚未接上它，详见 §五。

### 4.5 C 线 · 模型服务、测评集与评测（`scripts/`、`正式测评集/`、`eval/`）

| 文件 | 功能 |
| --- | --- |
| `scripts/pangu_env.sh` | 推理环境变量（CANN 8.3.RC1 路径、卡号等）；手工写全，不要用官方 `set_env.sh` |
| `scripts/pangu_infer.py` | 推理入口：命令行单轮问答 + 可 import 的 `PanguModel`（含 `chat_batch`） |
| `scripts/smoke_pangu.py` | 首次验证冒烟脚本 |
| `scripts/bench_pangu.py` / `bench_batch.py` | 单条 / 批处理测速脚本 |
| `正式测评集/*.jsonl` | 正式测评集，332 题 / 13 维度 |
| `eval/README.md` | 评测方案：生成 → 判分 → 报告三阶段 |
| `eval/run_eval.py` | 评测主脚本（读题库 → 调 `PanguModel` → 判分 → 出表） |
| `eval/eval_lib.py` | 题库加载、提示词构造、规则判分、配置指纹 |
| `eval/ai_judge.py` / `judge.py` | DeepSeek 复核判分（开放题 / rubric 题），结果按题缓存 |
| `eval/show_report.py` | 报告展示 |
| `eval/runs/` | 每次评测的原始输出与报告 |
| `run_eval.sh` | 一键评测包装脚本（`bash run_eval.sh --quick` 冒烟） |

## 五、三条线与接口现状

项目目标与分工见 `项目综述.md`，接口细节见 `智能体接口规范.md`。当前接线状态：

| 接口 | 契约 | 现状 |
| --- | --- | --- |
| C → B：模型服务 | `ChatOpenAI` 连 `OPENAI_BASE_URL`（默认 `http://localhost:8000/v1`） | ❌ 本机没有 OpenAI 兼容服务，8000 端口无监听；模型只以进程内 transformers 形式存在 |
| A → B：检索 | `retrieve(query, k) -> list[Evidence]` | ❌ B 侧 `agent/retrieval.py` 仍是返回 `[]` 的 mock；A 侧实现签名与 score 语义都对不上，需要适配层 |
| B → 评测 | 消费 `AgentResult` | ❌ `eval/` 只评裸模型（L0），从不调用 `answer()`；L1–L5 仅有接口预留 |

因此现在跑 `python -m agent.cli` 会因为连不上模型服务而失败，这是预期现象，不是代码坏了。
打通第 1 项（包一个 OpenAI 兼容 HTTP 服务）是解锁联调与 D07 评测题的前提。

另外：`eval/runs/L0-*-b32-pv1/` 目前只有 `skipped.jsonl`，L0 基线数据尚未产出。

## 六、常用命令

| 目的 | 命令 |
| --- | --- |
| 跑离线测试（83 项，约 0.6 秒） | `cd /data/openpangu && .venv/bin/python -m pytest -q` |
| 起智能体 CLI | `cd /data/openpangu && .venv/bin/python -m agent.cli` |
| 单轮模型推理 | `source scripts/pangu_env.sh && .venv-pangu/bin/python scripts/pangu_infer.py "问题……"` |
| 模型测速 | `.venv-pangu/bin/python scripts/bench_pangu.py` |
| 一键评测（冒烟） | `bash run_eval.sh --quick` |
| 一键评测（全量 L0，约 5 分钟） | `bash run_eval.sh` |
| 看 NPU 与显存 | `npu-smi info` |
| 看磁盘 | `df -h /data` |
| 看模型服务是否起来 | `curl http://127.0.0.1:8000/v1/models` |

### 智能体运行配置（环境变量或项目根 `.env`）

| 变量 | 默认 | 说明 |
| --- | --- | --- |
| `OPENAI_BASE_URL` | `http://localhost:8000/v1` | OpenAI 兼容服务地址（C 提供） |
| `OPENAI_API_KEY` | `not-needed` | 服务无鉴权时可随意填 |
| `MODEL_NAME` | `openpangu-7b` | 部署模型名 |
| `AGENT_MAX_TURNS` | `6` | 追问轮数上限，达上限强制收口 |
| `AGENT_TEMPERATURE` | `0.1` | 固定低温度 |
| `REVIEW_MODE` | `code` | `code` / `code+llm`（二期） |
| `REQUEST_TIMEOUT` | `60` | 模型请求超时（秒） |
| `AGENT_DISABLE_PROXY` | `true` | 构建客户端时忽略代理变量 |
| `AGENT_DEBUG` | `false` | 打印循环日志 |

## 七、已知坑（都踩过）

| 坑 | 说明 |
| --- | --- |
| 代理变量阻断模型客户端 | `ALL_PROXY / HTTP_PROXY / HTTPS_PROXY` 指向本地代理且环境缺 `socksio` 时，`ChatOpenAI` 构建失败；代码默认已 `trust_env=False`，手工调试时记得清空 |
| 平台禁用 `scp` | 传文件走 `base64` 或标准输入管道 |
| SSH 并发受限 | 连不上先等一分钟，或清掉本机僵死的 ssh 进程 |
| 没有 Docker | 服务只能裸装，别想着起容器 |
| 系统盘小 | `/` 只有 30G，大文件一律放 `/data` |
| 实例销毁 = 全清 | 只有 `/root` 与 `/data` 跨镜像保留 |
| 别 source 官方 `set_env.sh` | CANN 8.3.RC1 生成的脚本第一行有空格 bug，会把 `LD_LIBRARY_PATH` 写坏；用 `scripts/pangu_env.sh` |
| 卡号必须写 0 | 容器只暴露 `/dev/davinci1`，但容器内要设 `ASCEND_RT_VISIBLE_DEVICES=0` |
| 别在项目目录跑推理 | CANN 会在当前目录写 `fusion_result.json` 等临时文件，跑完清掉 |
| 权重 LFS 缓存已清理 | 为腾磁盘删了 `.git/lfs/objects`；工作区权重完好，误删后 `git lfs pull` 重下（17.6GB） |
| 旧模型名已下线 | Codex 侧 `deepseek-v4-flash` 已改名 `deepseek-flash`，配错起不来 |
| DeepSeek API Key | 在 `~/.codex/config.toml` 里，不要复制进项目文件、不要提交、不要打印到日志 |

## 八、本机 Codex

- 模型接 DeepSeek：`model = "deepseek-flash"`，思考强度默认 `high`，最高强度用 `codex -c model_reasoning_effort=max`。
- 配置文件 `/root/.codex/config.toml`（含 API Key），模型目录 `/root/.codex/models.json`。
- 已挂两个远程文档 MCP：`langchain-docs`、`langchain-reference`，写 LangChain / LangGraph 代码时可直接查。
- 切换模型或还原 `bash <(curl -fsSL https://cdn.deepseek.com/api-docs/codex-deepseek-setup-en.sh)`：1 = flash，2 = pro，9 = 还原。
- 沙箱：容器内不能建 user namespace，Codex 必须以 `danger-full-access` 运行。

## 九、文档变更

为让接手的人只读必要文档，以下文件已删除，内容已并入本文或代码：

| 已删除 | 内容去处 |
| --- | --- |
| `服务器环境说明.md` | 并入本文 §一 ~ §三、§七 |
| `交接文档.md` | 进度与断点并入 §五，已知坑并入 §七 |
| `智能体设计方案.md` | 危险信号与红线清单已在 `agent/tools/red_flag_rules.py`，行为与话术在 `agent/prompts/` |
| `A模块评审意见.docx`、`知识库模块要求（智能体侧）.docx` | 无代码依赖 |
