# 开发环境说明（Python 版 LangChain 工具包）

> 2026-09-12 安装并验证。三人各自电脑都要装一份，不共用。

## 一、环境位置

- 目录：`.venv/`（与本文档同目录）
- Python：3.13
- 包管理工具：uv（推荐），也可用 pip

在项目根目录激活环境：

```powershell
.\.venv\Scripts\Activate.ps1
```

## 二、已安装组件

| 组件 | 版本 | 对应分工 |
| --- | --- | --- |
| langchain | 1.4.0 | B：智能体与提示词 |
| langgraph | 1.2.11 | B：流程编排（分支、转接、状态管理） |
| langchain-openai | 1.6.2 | C：接入 openPangu 的 OpenAI 兼容接口 |
| langsmith | 0.12.4 | 全组：调用追踪与调试（随 langchain 一起装） |
| pytest | 9.1.1 | B：离线单元测试（无模型、无网络） |

其余依赖的精确版本见 `requirements.txt`。

## 三、验证记录

安装当日通过冒烟测试：编排图能正常构建并执行，智能体入口能正常导入。

2026-09-12 智能体代码完成后：离线测试 83 项全部通过（工具 / 审查规则 / 结构校验 / 假模型场景）。

## 四、复现方法

在各自电脑的项目目录执行：

```powershell
uv venv --python 3.13 .venv
uv pip install -r requirements.txt
```

没有 uv 时改用 pip：

```powershell
python -m venv .venv
.\.venv\Scripts\pip install -r requirements.txt
```

## 五、运行离线测试（B 线）

不需要模型服务与网络，随 requirements.txt 安装 pytest 后即可运行：

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

## 六、暂未安装（按各自进度再装）

- A 线：Chroma、jieba、rank_bm25、bge-m3、RAGAS
- C 线：vLLM / Ollama、Streamlit、scipy、datasets
