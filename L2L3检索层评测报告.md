# L2 / L3 检索层评测报告 · openPangu-R-7B-2512

> 日期：2026-09-14 ｜ 层级：L2（纯向量检索）/ L3（混合检索 + 标定阈值）
> 评测集：`正式测评集/`（332 题，本轮覆盖可自动跑的 312 题）
> 配套：`L0基线评测报告.md`（无检索基线）、`eval/README.md`（评测方案）、`kb/README.md` §八（检索侧指标）

## 一、结论速览

1. **L2 严格准确率 34.4%，比 L0 基线（49.8%）低 15.4 个百分点——但这个数字不能读成「检索有害」。**
   原因是输出被截断：312 题里 **168 题（54%）跑满了输出预算**（L0 只有 7 题），
   答案没写完就被判错。截断才是造成分数下滑的主因。
2. **只看两轮都答完的题，检索是有帮助的**：56 道在三轮里都没被截断的题上，
   L0 正确率 60.7%、L2 69.6%、L3 64.3%（n 小，仅作方向性参考）。
3. **引用可追溯率 72.4%**：有检索证据的题里，模型给出的 chunk_id 有七成以上
   确实来自本轮检索结果。这一项在本轮之前被一个正则 bug 钉死为 0.0%（见 §五）。
4. **当前 RAG 提示词会导致模型在慢思考里打转。** 这是下一步要修的头号问题，
   不修掉，L2/L3 的分数都不可解释。

## 二、实验配置

| 项 | L2 | L3 |
| --- | --- | --- |
| 检索策略 | 纯向量（余弦排序） | 向量 + BM25 → RRF 融合 |
| 余弦阈值 | 不设（全部候选入 prompt） | 0.55（`kb.calibrate` 标定） |
| 每题材上下文 | 5 条（312 题**全部**有） | 254 题拿满 5 条，32 题无证据（10.3%） |
| 思考模式 | 慢思考 | 慢思考 |
| 批大小 / 指纹 | 32 / `32a356bb128b` | 32 / `da8fae8db844`（污染轮） |

提示词把检索结果按 `[1] [2] …` 编号塞进用户消息，并要求用
`[chunk_id: kb::<doc>::<hash>]` 标注依据（见 `eval/eval_lib.py::format_rag_context`）。

## 三、总表

| 指标 | L0 裸模型 | L2 纯向量 | L3 混合检索 |
| --- | --- | --- | --- |
| 严格准确率 | **49.8%** | 34.4% | 43.7%（污染轮，见 §五） |
| 平均得分（含部分分） | 55.1% | 36.8% | 49.2% |
| 安全合规率（D09–D11） | 38.5% | 36.9% | 44.6% |
| 引用可追溯率 | 0（基线无检索，按口径固定） | **72.4%** | 71.4% |
| 触顶截断题数 | 7 / 312 | **168 / 312** | 105 / 312 |
| 未截断题的准确率 | 50.8% | 54.3% | 60.6% |

> 「未截断题的准确率」这一行有选择性偏差（各轮剩下的不是同一批题），
> 不能直接横向比；要用 §四 的同题口径。

### 3.1 分维度（严格准确率）

| 维度 | L0 | L2 | L3（污染轮） |
| --- | --- | --- | --- |
| D01 医学知识基础 | 67.5% | 35.0% | 47.5% |
| D02 病例诊断推理 | 80.0% | 45.0% | 77.5% |
| D03 鉴别诊断 | 32.0% | 32.0% | 28.0% |
| D04 检查决策 | 45.5% | 27.3% | 45.5% |
| D05 病例诊断分析 | 46.7% | 40.0% | 46.7% |
| D06 多轮问诊诊断 | 30.0% | 40.0% | 30.0% |
| D08 疑难病例 | 24.0% | **0.0%** | 12.0% |
| D12 临床计算 | 28.0% | 28.0% | 28.0% |
| D13 证据检索与溯源 | 65.0% | 60.0% | 55.0% |

D08 在 L2 上是 0/25——全部 25 题都跑满了预算（十选一的疑难病例本来就是最长的推理）。
完整表格以 `eval/runs/<run-id>/summary.md` 为准。

## 四、关键诊断：截断吃掉了结论

| 轮次 | 截断题数 | 占比 | 截断题的准确率 | 未截断题的准确率 |
| --- | --- | --- | --- | --- |
| L0 | 7 | 2.2% | 14.3% | 50.8% |
| L2 | 168 | 54.0% | 22.2%（153 道可判分） | 54.3% |
| L3（污染轮） | 105 | 33.7% | 21.0% | 60.6% |

**同题口径**（三轮都没被截断的 56 道题）：

| 轮次 | 正确 | 准确率 |
| --- | --- | --- |
| L0 | 34 | 60.7% |
| L2 | 39 | **69.6%** |
| L3（污染轮） | 36 | 64.3% |

也就是说：**只要模型把话说完了，RAG 是加分的；分数掉下来是因为它越来越多地说不完。**

现象（`predictions.jsonl` 可查）：加入【参考资料】后，慢思考的推理过程开始重复打转，
典型片段如「…448.8880 就是 448.8880…我认为这样可以…」直到把预算耗尽，
而 `do_sample=False` 的贪心解码一旦进循环就不会自己出来。

可能的修法（按代价排序，未实施）：

1. **要求先给结论再解释**——MCQ 占截断题的一大半，只要「答案：X」出现在输出开头，
   后面的推理即使被截断也不影响判分；
2. 给 RAG 层的输出预算单独放大（`run_eval.sh` 已支持 `--budget-scale`，重跑只补截断题）；
3. 压缩上下文条数（k=5 → 3）或限制每条长度；
4. 换解码参数（重复惩罚）——会破坏「贪心、可复现」的约定，最后才考虑。

## 五、本轮修掉的两个 bug（都会静默产出错误结论）

### 5.1 引用解析匹配不到 chunk_id（已修）

`eval/traceability.py` 的正则字符集不含冒号，而真实 chunk_id 形如
`kb::baike-38::3badd7513c40`，导致引用可追溯率恒为 0.0%。修复后同一批 L3 结果
从 0.0% → 76.2%。现在两种写法都认：显式 `[chunk_id: …]` 和资料编号 `[1]`。

### 5.2 预检索半成品被复用（已修）

一次被中断的 L3 预检索只写了 19 题 `contexts.jsonl` 就死了。重跑时
`ensure_contexts` 发现行数不够、去调 `stage_retrieve`，而后者见到文件存在直接跳过，
于是 **298 道题拿到的都是「本轮未检索到相关资料」**，整轮实验静默失真
（这一轮已归档为 `eval/runs/L3-slow-b32-badctx-131825/`，保留可查）。

修法：`ensure_contexts` 改为按题号检查覆盖，缺题就带 `force=True` 重跑，
重跑后仍缺则直接报错退出；`tests/test_eval_contexts.py` 守住这三条行为。

**操作习惯**：跑完 L2/L3 先看 `contexts.jsonl` 的行数与平均证据条数，再看分数。

## 六、检索侧指标（A 线，110 条评测集）

| 口径 | 结果 |
| --- | --- |
| chunk 级 Recall@5（阈值 0.55） | 0.600（54/90） |
| 文档级 Recall@5 | 0.778（70/90） |
| 无关问题误召率 | 0.050（1/20） |
| 中文卷题命中 | 0.797（51/64） |
| 英文卷题命中 | 0.115（3/26） |

两个待决策项：**英文卷（Europe PMC CC BY，26 题）几乎召不回**，
要么加跨语言检索通道，要么承认它只作背景；**L2 不设阈值导致 312 题全部塞进 5 条资料**，
其中相当一部分与问题无关——这正是 L2 截断率最高的原因之一。

顺带一条对照（L3 重跑轮，阈值 0.55 生效后）：312 题里 254 题拿满 5 条、32 题完全没有证据，
「有证据」的题平均 4.73 条。也就是说阈值在真实题目上主要起「过滤掉整题」的作用，
而不是「每题少给几条」——它把 10% 的题挡在门外，其余的照常给满。

## 七、当前状态与下一步

| 项 | 状态 |
| --- | --- |
| L2 慢思考全量（312 题） | ✅ 完成，`eval/runs/L2-slow-b32/` |
| L3 慢思考全量 | 🔄 重跑中：预检索已完整（312 题，修复生效），生成阶段进行中；污染轮已归档为 `L3-slow-b32-badctx-131825` |
| L0 基线 | ✅ `eval/runs/L0-slow-b32/`、`L0-fast-b32/` |
| L1 / L4 / L5 | ❌ 未实现——需要先把 `eval/` 接到 `agent.answer()` |
| D07 智能体问诊（15 题） | ❌ 需要先实现「按隐藏信息作答的模拟患者」 |
| D13 检索题（5 题） | ❌ 金标准在 CmedqaRetrieval 语料里，需单独索引该语料 |

建议顺序：**先修 §四 的提示词截断问题 → 重跑 L2/L3 → 再谈 L1 与 L4/L5**。
在截断修好之前，L2/L3 的分数不具备对外汇报的价值。

```bash
# 看 L3 重跑进度与结果
bash watch.sh L3-slow-b32
.venv-pangu/bin/python eval/show_report.py --run-dir eval/runs/L3-slow-b32

# 只补跑被截断的题（放大预算）
bash run_eval.sh --layer L3
.venv-pangu/bin/python eval/run_eval.py --stage generate --run-id L3-slow-b32 \
    --dataset-dir 正式测评集 --layer L3 --thinking slow --batch-size 32 --budget-scale 2
```
