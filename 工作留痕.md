# 工作留痕

> 每完成一轮工作追加一节，倒序排列（最新在最前）。只记「做了什么、怎么验的、留了什么」，
> 不记过程性思路。项目约定见 `AGENTS.md`。

## 2026-09-14（下）· 阶段结果汇总（评测已暂停）

### 暂停点

按用户要求暂停全部评测：智能体全量那轮停在 **16/312**（`eval/runs/AGENT-slow-4x/`），
模型服务仍在运行（`bash scripts/serve_pangu.sh status`）。已跑完的轮次结果都在
`eval/runs/<run-id>/`，报告快照已归档到 `评测报告/`。

### 结果总表（严格准确率 / 平均得分含部分分）

| run | 层级·思考 | 可判分 | 截断 | 救援 | 准确率 | 均分 | 安全合规 | 引用可追溯 | 未截断题口径 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `L0-slow-b32` | L0 慢 | 247 | 7 | 0 | 49.8% | 55.1% | 38.5% | 0（口径） | 50.8% / 56.2%（240 题） |
| `L0-fast-b32` | L0 快 | 247 | 5 | 0 | 44.9% | 49.8% | 50.8% | 0（口径） | 45.1% / 50.0%（244 题） |
| `L2-slow-b32` | L2 慢 | 247 | 168 | 0 | 34.4% | 36.8% | 36.9% | 72.4% | 54.3% / 59.0%（94 题） |
| `L3-slow-b32-badctx` | L3 慢·旧协议 | 247 | 105 | 0 | 43.7% | 49.2% | 44.6% | 71.4% | 60.6% / 68.0%（142 题） |
| `L3-slow-v3` | L3 慢·thinkcap-v1 | 247 | 0 | 163 | 40.9% | 46.0% | 41.5% | 67.5% | 56.6% / 62.1%（99 题） |
| `L3-fast-b32` | L3 快·冒烟 | 18 | 0 | 0 | 50.0% | 50.4% | — | 76.2% | 同左 |
| `smoke-L3-v3` | L3 慢·冒烟 | 18 | 0 | 11 | 38.9% | 40.6% | — | 81.0% | 57.1% / 61.5%（7 题） |
| `AGENT-slow-4x`（暂停） | 智能体·4× 预算 | 16 | 0 | 0 | 0.0% | — | — | — | 见下方口径说明 |

「未截断题口径」是**有偏的上限**：被截断的恰好是最长最难的题，剔掉它们等于只统计模型顺手的题，
不能对外当真实水平。L3 的真实区间大致落在 41%–61%，取决于怎么处理没答完的题。

### A 线检索侧（110 条评测集，阈值 0.55）

| 口径 | 结果 |
| --- | --- |
| chunk 级 Recall@5 | 0.600 |
| 文档级 Recall@5 | 0.778 |
| 无关问题误召率 | 0.050 |
| 中文卷 / 英文卷命中 | 0.797 / 0.115 |

### 这一阶段暴露的三个坑（都已修）

1. 引用正则不含冒号 → 引用可追溯率恒为 0（同一批数据 0.0% → 76.2%）；
2. 预检索半成品被复用 → 298 题静默变成「无资料」；
3. 「思考上限 = 预算一半」误掐 44% 本来能答完的题 → 一半题目被降级成快思考补答，分数被压低。
   现已改为**默认不设思考早停线**（思考时长由 max_new_tokens 与复读检测约束）。

### 智能体路径的口径问题（要决策）

把测评集题目交给面向患者的智能体时，它按「问诊」作答，不会输出基线评分器要找的
`答案：X`，所以 MCQ 类（D01/D02/D08）在基线口径下必然是 0 分——16 题实测准确率 0.0%。
这不是模型不会，是**评分口径不匹配**。三个选项：

1. 给智能体链路单独写评分适配（从 `assessment.hypotheses` 取主诊断、与选项比对）；
2. 智能体只跑开放题维度（D03/D04/D05/D06/D09–D11），选择题仍走基线路径；
3. 接受「口径不匹配」的原始结果，只当作链路可用性验证。

### 速度对照（决定后面怎么跑）

| 路径 | decode 速度 | 312 题预计 |
| --- | --- | --- |
| 基线批量路径（batch 32，512 token 输出） | 754 tok/s 总吞吐 | 10–15 分钟 |
| 智能体路径（服务端单条串行） | 37 tok/s | 约 2 小时（实测 18–24s/题） |

智能体路径要提速，需要把服务端的**收口轮批处理化**（多题同一轮一起解码），预计可压到 20–30 分钟。

## 2026-09-14 · 仓库安全 + 四个静默 bug + 生成协议 + 检索评测集

### 做了什么

1. **堵仓库安全口子**：`.gitignore` 补 `kb/corpus_raw/` 三个数据目录。此前
   `git add -A`（`sync.sh` 就是这么干的）会把 647MB 原始语料、含单个 563MB 文件
   推进公开仓库。待提交体积 647MB → 1.4MB。
2. **修 4 个会静默产出错误结论的 bug**：
   - `eval/traceability.py` 引用正则不含冒号 → 真实 chunk_id 匹配不到，引用可追溯率恒为 0；
   - `eval/run_eval.py` 复用被中断的半成品 `contexts.jsonl` → 298 题静默变成「无资料」；
   - `kb/eval_set.py` 抽样只取索引前 40 条、只给 doc 级 gold → 取样集中且等于不测排序；
   - 上一条同时修 `--limit` 语义（原为调试开关）。
3. **A 线补完**：生成 `kb/eval/queries.jsonl`（110 条，chunk 级 gold），
   标定确认 `KB_SCORE_THRESHOLD=0.55`，并量出跨语言短板（中文卷 0.797 / 英文卷 0.115）。
4. **新增生成协议 `thinkcap-v1`**：RAG 层 +768 token 预算；慢思考最多用掉一半；
   思考块内 24-gram 复读即逐行掐断；被掐断的题用快思考补答一次（每题最多一次）。
5. **文档口径统一**：`AGENTS.md`、`README.md`、`环境说明.md`、`智能体接口规范.md`（升 v1.2）、
   `eval/README.md`、`kb/README.md`、`setup/README.md`。
6. **报告归档**：新增 `L2L3检索层评测报告.md`、`评测报告/`（历次报告快照 + 收集脚本）。

### 怎么验的

| 验证 | 结果 |
| --- | --- |
| 离线测试 | 190 → **228 项全绿**（新增 `test_traceability.py` 13、`test_kb_eval_set.py` 7、`test_eval_contexts.py` 4、`test_think_limit.py` 14） |
| A→B 检索真实冒烟 | `kb.inspect` 在 84,359 块索引上正常返回带 chunk_id 的证据 |
| 阈值标定 | 9 档阈值 × 110 题，0.55 为唯一同时满足误召率与召回的点（误召 5%） |
| 引用可追溯率修复 | 同一批 L3 结果 0.0% → 76.2% |
| 思考限制（真机） | 24 题慢思考冒烟截断 **0**（旧协议约 34%）；6 道此前必错的 D12 计算题掐断 6/6、补答 6/6；原先被误判为「打转」的正常答案复跑后自然结束 |

### 留了什么

- 提交：`ddce092`（修复 + 协议 + 评测集）、`cc33d90`（文档 + 测试计数），见 `git log`。
- 推送：`528a968`（报告归档）已推到 `origin/main`。
- 报告：`L0基线评测报告.md`、`L2L3检索层评测报告.md`、`评测报告/<run-id>/`。
- 后台任务：`*-slow-v3`（L0 / L2 / L3 全量新协议重跑），日志 `logs/eval_v3.log`；
  另挂了自动归档守护：三轮跑完自动 `评测报告/collect.sh` + 提交 + 推送，日志
  `logs/collect_v3.log`。

### 没做完（下一步）

1. `*-slow-v3` 三轮跑完后：用 `bash 评测报告/collect.sh` 补进 `评测报告/`，
   把 `L2L3检索层评测报告.md` 的旧协议数字换成新协议数字。
2. L1 / L4 / L5 仍未实现——需要先把 `eval/` 接到 `agent.answer()`（目前只评裸模型）。
3. D07 智能体问诊 15 题需要「按隐藏信息作答的模拟患者」；D13 检索 5 题需要单独索引
   CmedqaRetrieval 语料。两者都还在跳过状态（见各轮 `skipped.jsonl`）。
4. 医学内容待评审：`agent/tools/red_flag_rules.py`（自标 v0.1）、急诊/自伤固定话术、
   289 道机器翻译题。
