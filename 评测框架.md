# 长期测评框架

> 2026-09-14 ｜ 替代此前散落的 `run_eval.sh` / `watch_all.sh` / 手工拼命令。
> 一句话：**一次实验 = 一份配置 + 一个不可变的 run 目录 + 一份不可变的报告**。

## 一、目录约定

| 位置 | 放什么 | 进 git 吗 |
| --- | --- | --- |
| `eval/` | 引擎（怎么算）：`run_eval.py`（L0–L5 直连）、`run_agent_batch.py`（智能体批处理）、判分与展示 | ✅ |
| `eval/harness/` | 框架（怎么组织）：配置解析、不可变 run、报告归档、登记表 | ✅ |
| `eval/configs/*.yml` | 实验定义：层级 / 引擎 / 预算 / 批大小 / 检索条数 / 题目范围 / 是否 AI 判分 | ✅ |
| `eval/scripts/` | 启动脚本：`run.sh`、`watch.sh`、`stop.sh` | ✅ |
| `eval/runs/<run-id>/` | **过程数据**：predictions / scores / contexts / rubric 判定 / progress / harness.log / meta.json | ❌（体积大，可重跑） |
| `评测报告/<run-id>/` | **报告**：summary.md、summary.json、report.html、compare.md、skipped.jsonl、meta.json | ✅ |
| `评测报告/INDEX.md` | 登记表：每跑一次追加一行，**只增不改** | ✅ |

## 二、不覆盖是怎么保证的

1. **run id 自带时间戳与指纹**：`20260914T152007_agent-batch-4x_a1b2c3d4`
   （UTC 时间 + 实验名 + 配置指纹 8 位）。同名实验、不同参数也不会撞。
2. **建目录时先检查**：已存在直接报错退出（`RunExistsError`），框架不提供强制覆盖开关。
3. **报告归档不覆盖**：`评测报告/<run-id>/` 里已有同名文件就跳过写入。
4. **登记表只追加**：`INDEX.md` 每次追加一行，历史行不动。
5. **过程数据只增**：同一 run 内支持断点续跑（已完成的题会跳过），但不会写进别人的目录。

## 三、怎么跑

```bash
# 1) 看有哪些实验
bash eval/scripts/run.sh --list

# 2) 跑一个实验（结果自动归档 + 追加登记表）
bash eval/scripts/run.sh eval/configs/agent-4x.yml

# 3) 冒烟：每维度 2 题、不跑 AI 判分，约 1 分钟
bash eval/scripts/run.sh eval/configs/agent-smoke.yml

# 4) 只看计划不执行
bash eval/scripts/run.sh eval/configs/l3-hybrid.yml --dry-run

# 5) 临时覆盖参数（不改配置文件）
bash eval/scripts/run.sh eval/configs/agent-4x.yml --limit 64 --retrieval-k 5

# 看进度 / 停止
bash eval/scripts/watch.sh                 # 默认看最新一次
bash eval/scripts/watch.sh 20260914T1520   # 按前缀匹配
bash eval/scripts/stop.sh                  # 停评测；--all 连模型服务一起停
```

智能体档需要模型服务在跑（4 倍预算）：

```bash
TOOL_MAX_TOKENS=1280 FINALIZE_MAX_TOKENS=3600 bash scripts/serve_pangu.sh start
```

## 四、加一个新实验

在 `eval/configs/` 里加一个 yml 即可，**不用改代码**：

```yaml
name: agent-batch-4x-k5     # 进 run id，用英文短横线
engine: agent-batch         # direct（L0–L5 直连）| agent-batch（智能体批处理）
layer: AGENT                # agent-batch 固定 AGENT
budget_scale: 4.0           # 输出预算倍数（1.0 = 基准）
retrieval_k: 5              # RAG 每题给几条（「给多少资料」的旋钮）
batch_size: 32
judge: true
notes: 对照实验：RAG 给 5 条
```

常用覆盖项：`per_dimension`（每维度题量）、`dimensions`（只跑某些维度）、`limit`、
`judge`（是否 AI 判分）、`judge_workers`。

## 五、每次运行留下什么

`eval/runs/<run-id>/meta.json`（过程数据侧）与 `评测报告/<run-id>/meta.json`（报告侧）内容一致：

| 字段 | 含义 |
| --- | --- |
| `run_id` / `started_at_iso` / `finished_at_iso` | 标识与时间 |
| `config_file` / `config` / `params` / `fingerprint` | 这次到底怎么跑的（可复现） |
| `commands` | 实际执行的完整命令 |
| `git_commit` | 当时的代码版本 |
| `status` | running / done / failed |
| `note` | 失败时的退出码等 |

`INDEX.md` 一行一次运行：run-id、完成时间、实验、层级/引擎、关键参数、题目数、
准确率、均分、安全合规、引用可追溯、报告链接。

## 六、和旧脚本的关系

旧的 `run_eval.sh` / `watch.sh` / `watch_all.sh` / `stop_eval.sh` 仍然可用（底下的引擎没变），
但**新实验一律走 `eval/scripts/run.sh`**——它才会写 meta、归档报告、追加登记表，
并且保证不覆盖历史。旧脚本跑出来的 run 目录后续可用
`bash 评测报告/collect.sh <run-id>` 补一份报告快照。

## 七、当前可用的实验配置

| 配置 | 引擎 | 说明 | 预计耗时（312 题） |
| --- | --- | --- | --- |
| `l0-baseline.yml` | direct | 裸模型基线（L0） | 10–15 分钟 |
| `l3-hybrid.yml` | direct | RAG 混合检索（L3），4 倍预算 | 15–25 分钟 |
| `agent-4x.yml` | agent-batch | 智能体全链路，4 倍预算，RAG 3 条 | 约 26 分钟 |
| `agent-smoke.yml` | agent-batch | 每维度 2 题冒烟，不跑 AI 判分 | 约 1 分钟 |
