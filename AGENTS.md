# AGENTS.md · 本项目在这台服务器上的工作约定

> 改动本项目前先读这份文件，再读 `项目综述.md`（做什么）和 `环境说明.md`（环境与文件地图）。

## 一、这是什么项目

「面向医疗诊断场景的 openPangu 能力增强方案」—— 2026 中国国际大学生创新大赛 · 华为企业命题。
用 RAG + 智能体增强 openPangu 在医学诊断场景的回答：**结论准确、每条可追溯、高风险问题守边界**。
项目一页纸见 `项目综述.md`。

## 二、文档导航

本项目只保留四份文档，各有单一职责：

| 文件 | 内容 |
| --- | --- |
| `项目综述.md` | 项目一页纸：做什么 / 怎么做 / 谁做 / 怎么证明 |
| `环境说明.md` | **机器实况 + 两个 Python 环境 + 每个文件的功能 + 接口现状 + 命令 + 已知坑（干活前必读）** |
| `智能体接口规范.md` | 三条线之间的接口契约（签名 / 字段 / 枚举 / 错误语义）；改接口先改这里，按 §6 记录变更 |
| `模型部署说明.md` | openPangu 怎么跑、实测速度、批处理、部署坑 |

代码结构导航见 `环境说明.md` §四；当前接口断点见 §五。

## 三、三条线与当前状态

| 线 | 模块 | 成果 | 状态 |
| --- | --- | --- | --- |
| A | 知识库与检索 | `rag_med_project/` | 检索代码跑通，但只有测试文档；接口与 B 侧契约不一致 |
| B | 智能体与提示词 | `agent/`、`tests/` | 一期代码完成，83 项离线测试全绿；未接真实模型 |
| C | 模型服务与评测 | `scripts/`、`正式测评集/`、`eval/` | 模型已在本机跑通；评测脚本就绪，但只评裸模型（L0） |

**最大断点：B 线的 `ChatOpenAI` 连不上本机模型**（没有 OpenAI 兼容 HTTP 服务），
所以 `python -m agent.cli` 现在必然失败。详见 `环境说明.md` §五。

## 四、工作约定

1. 动 `agent/` 或 `tests/` 之后必须 `.venv/bin/python -m pytest -q` 全绿（当前 83 项，约 0.6 秒跑完），不绿不算完成。
2. 动 `agent/prompts/` 下任何文件：先更新 `agent/prompts/CHANGELOG.md`，再跑 `tests/test_scenarios_fake_model.py` 回归。
3. 改接口先改 `智能体接口规范.md`，按其中 §6 记录变更；字段与枚举「只增不改」。
4. 与用户对话、写文档、写注释一律用中文；文档风格照 `项目综述.md`：短句、表格、少形容词。
5. 数据来源要注明：测评集出自 CMExam / CMB-Clin（Apache-2.0）与 IMCS-V2（MIT）。
6. DeepSeek API Key 在 `~/.codex/config.toml` 里，不要复制进项目文件、不要提交、不要在日志里打印。
7. `git commit` 之前先问用户。

## 五、这台机器

硬件、系统、两套 Python 环境、常用命令、已知坑全部写在 `环境说明.md`，动手前扫一遍即可。最常用的两条：

- 跑测试：`cd /data/openpangu && .venv/bin/python -m pytest -q`
- 跑模型：`source scripts/pangu_env.sh && .venv-pangu/bin/python scripts/pangu_infer.py "问题……"`

## 六、不要做的事

- 不要碰 `systemctl`、不要 `reboot`、不要把 `/` 写满。
- 不要用 Docker / podman 起服务：容器里没有，也装不上。
- 不要把模型权重放 `/root`：17.6GB，必须放 `/data`。
- 不要自己另选部署版本组合：CANN 与推理环境的可用组合见 `模型部署说明.md`。
- 不要在项目根目录跑模型推理（CANN 会写临时文件，跑完清掉）。
