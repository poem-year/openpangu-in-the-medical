# 服务器环境说明（模力方舟 · 昇腾容器）

> 更新：2026-09-13 ｜ 配合 `AGENTS.md`、`交接文档.md` 使用

## 一、机器

| 项 | 值 |
| --- | --- |
| 平台 | 模力方舟（moark.com）算力容器，按小时计费 |
| 连接 | `ssh root+vm-apyFdFLehNlmwXBf@106.75.235.239 -p 32222`（VS Code Remote-SSH 用同一地址） |
| 架构 | x86_64，128 核 |
| 内存 | 64GB |
| NPU | 昇腾 910B2C × 1，健康状态 OK，64GB HBM（`npu-smi info` 查看） |
| 系统 | openEuler 24.03 LTS-SP2 |
| CANN | 9.0.0（安装于 `/usr/local/Ascend/cann-9.0.0`） |
| Python | 3.11.15（`/usr/local/python3.11.15/bin/python3`），另有 `/opt/conda` 可选 |
| 磁盘 | `/` 30G（已用 1.9G）、`/data` 59G（几乎全空）——大文件一律放 `/data` |
| 已装工具 | git / curl / wget / tar / gcc 工具链 |
| 没有 | Docker / podman / buildah（容器内也装不上） |
| 持久化 | `/root`、`/data` 跨镜像保留；实例销毁则全清；关机保 36 小时 |

## 二、目录约定

| 路径 | 用途 |
| --- | --- |
| `/data/openpangu` | 本项目工作目录（文档、代码、评测集） |
| `/data/models` | 模型权重放这里（建议路径，约 17.6GB） |
| `/data/huggingface_home` | 平台预置的 HuggingFace 缓存目录 |
| `/data/openpangu/.venv` | 项目 Python 环境（3.11.15，依赖已装好，测试已跑通） |
| `/root/.codex` | Codex 配置（DeepSeek）与备份 |
| `/root/bin/codex` | `codex` 命令包装脚本（指向 VS Code 扩展自带的程序本体） |
| `/root/codex-env-bundle/` | 环境迁移包，换机器时用 |

## 三、Codex 与模型

- 模型接的是 DeepSeek，由官方接入脚本 v1.3.0 写入：`model = "deepseek-flash"`，提供方 `deepseek`，走 Responses API
- 配置文件 `/root/.codex/config.toml`（含 API Key）；模型目录 `/root/.codex/models.json`（`deepseek-flash`、`deepseek-v4-pro`）
- 备份：`/root/.codex/backup-deepseek/config.toml`（安装前的原配置）、`/root/.codex/models.json.legacy-backup`
- 思考强度默认 `high`；要最高强度用 `codex -c model_reasoning_effort=max`
- MCP：已挂两个远程文档服务 `langchain-docs`、`langchain-reference`，写 LangChain / LangGraph 代码时可直接查文档
- 切换模型或还原：`bash <(curl -fsSL https://cdn.deepseek.com/api-docs/codex-deepseek-setup-en.sh)`，1 = flash，2 = pro，9 = 还原

## 四、常用命令

| 目的 | 命令 |
| --- | --- |
| 看 NPU 与显存 | `npu-smi info` |
| 看磁盘 | `df -h /data` |
| 看 Python 版本 | `python3 -V` |
| 跑离线测试 | `cd /data/openpangu && python -m pytest -q` |
| 起 Codex | `codex` |
| 看模型接口是否起来 | `curl http://127.0.0.1:8000/v1/models` |

## 五、网络与平台限制

- 可访问：`api.deepseek.com`、`cdn.deepseek.com` 等外网地址
- 平台不支持 `scp`：传文件走 `base64` 或标准输入管道
- 并发 SSH 会话数受限：连不上先等一分钟，或清掉本机僵死的 ssh 进程再试
- pip 已配清华镜像，`pip config list` 可查；要直连官方源时加 `-i https://pypi.org/simple`